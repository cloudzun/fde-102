# 实操三：Delta 施工——政务政策知识问答系统（场景 B）

> **对应课纲：** FDE 综合实操 · 实操三（Delta 视角 · 场景 B）
> **时长：** 约 120 分钟
> **角色：** 你继续扮演 **Delta（FDE/FDSE）**——手上沾泥，把方案变成能跑的代码
> **工作流：** gstack 八环节（office-hours → spec → autoplan → build → review → qa → ship → retro）
> **前置：** 已完成实操一、实操二，组内拥有《解决方案框架》与实操二的《AGENTS.md》

---

## 实操概述

实操二，你作为 Delta 把场景 A（诉求分类）做出来了，坐席分派效率大幅提升。陈主任看到成效，催着把下一个子场景推上去。

这一轮，你要施工**场景 B：政务政策知识问答**。回想实操一的调研——市民问政策，坐席靠经验和翻文件回答，政策文件多、常更新、口径不一，答错了坐席担责任。陈主任的要求很明确：

> **"让市民问政策，系统直接给答案，还得告诉他是哪份文件、哪条规定的，免得坐席背锅。"**

> **你的施工任务书**是实操一《解决方案框架》里的场景 B。这一轮你不再是"想清楚做什么"的人，而是"把它做出来"的人。

**目标**：用 AI（opencode）在 2 小时内，做出一个**政务政策知识问答系统**——市民/坐席输入一个政策问题（如"外地户口能在西岭交社保吗"），系统从政策文件库检索相关内容，生成精确回答，并**标注答案来源**（哪份文件、哪一条），带一个 Streamlit 对话界面。

**技术方案不变**（来自实操一选型）：
- **RAG**（不是微调、不是纯关键词搜索）——政策文件会更新、答案必须可溯源、不能凭模型记忆瞎答。
- 技术栈：opencode + gstack 工程流程 + DeepSeek + 向量库 + Streamlit。

> **本实验的能力定位：** 它解决"**依据是什么**"——从政策库检索证据，生成可追溯回答。在附加高级实验 Lab4B 中，这一能力会被封装为 **`search_policy` 工具**，只有任务确实需要政策依据时才调用。Lab4B 复用的是**数据、证据纪律和工具契约**，不要求复用本实验的项目代码或向量库实现——它甚至可能用更轻量的检索方案（见实操三第 4 节复盘说明）。本实验的重点，是守住"没有证据不回答、来源可以核对"这条底线。

```mermaid
graph LR
    OH["office-hours<br/>确认该做"] --> SP["spec<br/>死磕边界"]
    SP --> AP["autoplan<br/>决策拍板"]
    AP --> BL["build<br/>按spec实现"]
    BL --> RV["review<br/>两遍检查"]
    RV --> QA["qa<br/>真实跑一遍"]
    QA --> SH["ship<br/>规范收尾"]
    SH --> RT["retro<br/>复盘"]

    style OH fill:#4C78A8,color:#fff
    style SP fill:#72B7B2,color:#fff
    style AP fill:#F58518,color:#fff
    style BL fill:#E45756,color:#fff
    style RV fill:#B279A2,color:#fff
    style QA fill:#54A24B,color:#fff
    style SH fill:#B279A2,color:#fff
    style RT fill:#FF9DA6,color:#333
```

---

## 本轮的核心学习点：RAG 的"答案可追溯"

这一轮，你延续实操二的工作方式——写启动提示词，交给 Agent 跑 gstack 八环节，你在 checkpoint 把关。**流程和实操二一样，但技术场景变了：从"一次调用"变成"检索 + 生成"。**

**RAG 和分类器的本质区别：**

| | 实操二 分类器 | 实操三 RAG |
|---|---|---|
| 输入 | 一句诉求 | 一个问题 |
| 输出 | 一个类别 | **一段回答 + 来源引用** |
| 核心 | 一次 API 调用 | **先检索（找相关文件）→ 再生成（基于文件回答）** |
| 关键挑战 | 分得准不准 | **答得准 + 来源可追溯** |

> **为什么"答案可追溯"是政务场景的命门？** 市民问政策，AI 答错了，坐席/平台要负责。如果 AI 的答案能溯源到"XX政策 第X条"，市民可以去核对，坐席也可以说"这是按文件规定来的"。**来源引用不是锦上添花，是政务 AI 的准入门槛。** 这正是实操一里李工说的"免得坐席背锅"。

---

## 知识背景：RAG 是什么

**RAG（Retrieval-Augmented Generation，检索增强生成）** 的核心思想一句话：**"考试时翻开书找答案，而不是把书背进脑子。"**

- 普通 LLM：凭记忆回答——可能记错、记漏、记混（尤其在政策这种常更新的内容上）
- **RAG**：先检索相关政策文件片段，再基于片段回答——**答案可溯源、且政策更新只需重建索引**

**RAG 的核心链路：**

```
政策文档 → 分块 → 向量化 → 存入向量库
                                    ↓ 用户提问
                    问题向量化 → 检索 top-K 相关片段
                                    ↓
                 组装 prompt（系统提示 + 片段 + 问题）→ LLM 生成答案 + 标注来源
```

| 概念 | 一句话 | 本实操 |
|------|--------|--------|
| **分块（chunking）** | 把长文档切成小段 | 按语义/段落切，块太小截断信息、太大检索不准 |
| **向量化（embedding）** | 把文字变成数字向量 | bge-m3 等中文 embedding 模型 |
| **向量库** | 存向量+原文，支持相似度检索 | Chroma 本地存储 |
| **检索** | 找到和问题最像的片段 | top-3 余弦相似度 |
| **引用** | 答案必须标来源 | 政策文号 + 条款 |

**RAG vs 微调 vs 纯关键词搜索（选型判断）：**

| 方案 | 适用 | 为什么本场景不用 |
|------|------|----------------|
| **RAG** | 文档常更新、答案需溯源 | ✅ 政策文件常更新，RAG 只需重建索引；答案天生带引用 |
| 微调 | 模型要"学会"固定领域知识 | ❌ 政策一更新就要重训；答案无法溯源；易幻觉 |
| 纯关键词搜索 | 问题→文档字面匹配 | ❌ "外地户口能交社保吗"和"灵活就业人员参保条件"有语义鸿沟，关键词匹配不上 |

---

## 0. 课前准备

- [ ] opencode Desktop 已安装并启动
- [ ] gstack 已安装（实操二装过，可复用）
- [ ] DeepSeek API key 已配置（`DEEPSEEK_API_KEY`）
- [ ] **向量库与 embedding 依赖已装**（详见下方）
- [ ] 已通读实操一的《解决方案框架》场景 B 部分
- [ ] 已查看实操二的《AGENTS.md》和《retro.md》（交接经验）
- [ ] 创建本实操目录 `实操三-政策问答/`

### 环境准备：向量库与 embedding 依赖

RAG 比分类器多了两样东西——**向量库**和 **embedding 模型**。让 Agent 帮你装，或手动装：

```
帮我安装 RAG 需要的依赖到当前 Python 环境：
1. 创建一个独立的虚拟环境 venv（不要复用之前的项目环境）
2. 安装向量数据库（推荐 chromadb）
3. 配置 embedding 服务：优先走 API（如硅基流动的 bge-m3），把 base_url 和模型名写进 .env
4. 安装完成后，写一行代码验证：能成功调用 embedding 并得到向量维度
```

> **提示：** embedding 有两条路——本地跑（下载模型）或走 API。
> - **本实操用 API 优先**（如硅基流动 bge-m3）。理由：实操二已确认本项目数据可出域，且本地跑 bge-m3 要下载 2GB+ 模型和 torch，机器配置低容易卡死。
> - **验证标准：** 装完后必须实际跑一行 `import` + 调用，确认能返回向量。注意 `pip list` 显示"已安装"可能是假象（全局环境变量泄漏导致加载到残缺包），**务必实测导入**，不要只看安装列表。
> - 若 API 不可用（无网络/无额度），可降级本地模型，但要预期下载时间和性能开销。

### 政策文档（Lab3-policy-docs 素材目录）

> **政策文档放在 `综合实操/Lab3-policy-docs/` 目录下，由讲师随手册一起提供。施工时让 Agent 从该目录读取全部文档，不要让 Agent 自己编政策内容。**

政策库覆盖市民高频咨询的 5 个领域，共 5 份文档，**混合格式**：

| 文件 | 格式 | 内容 |
|------|------|------|
| `社保参保政策.md` | Markdown | 6章27条：参保范围/缴费/外地户口参保/转移接续/监督 |
| `医保报销政策.md` | Markdown | 7章28条：住院/门诊/异地就医/报销办理/监督 |
| `保障性住房政策.md` | Markdown | 7章27条：申请条件/租金补贴/审核轮候/配租退出 |
| `户籍迁移政策.docx` | **Word** | 7章23条：落户条件/办理时限/所需材料/子女随迁 |
| `就业创业补贴政策.pdf` | **PDF** | 6章33条：失业保险金/创业担保贷款/社保补贴/培训补贴 |

> **格式说明：** md 文档可直接读取；`.docx` 和 `.pdf` 需要先做格式解析（转成可检索文本），才能分块向量化。

---

## 1. 写启动提示词（20 分钟）★本轮核心

> **这不是让 AI 干的活，是你（Delta）的活。** 你要把实操一的方案框架（场景 B 部分），翻译成一段能让 Agent 开跑的 office-hours 启动信息。流程和实操二一样，但这次你有了实操二的经验——**该写什么，你心里应该有数了。**

### Step 1：先理清"要交给 Agent 哪些信息"

和实操二一样，一份好的启动信息要包含 7 项。但**场景 B 的内容不同**，对照实操一方案框架场景 B 填：

| # | 要包含的信息 | 从方案框架哪里找 | 启动信息里怎么写 |
|---|------------|----------------|----------------|
| 1 | **背景与角色** | 你是谁、在给谁做 | "我是 FDE 团队的 Delta，施工场景 B：政务政策问答" |
| 2 | **要做什么** | 场景 B 定义 | "做一个政务政策知识问答系统，基于政策文件回答市民咨询" |
| 3 | **输入输出** | 场景 B 的"解决什么" | "输入一个政策问题，输出：回答 + 来源引用（哪份文件、哪一条）" |
| 4 | **技术选型（含理由）** | 场景 B 选型 | "已选型 RAG，不用微调（政策常更新、需溯源）、不用纯关键词搜索（语义鸿沟）" |
| 5 | **数据现状** | 场景 B 的数据风险 | "有 5 份政策文档（见下）+ Q&A 验证集；政策会更新，需重建索引" |
| 6 | **验收口径** | 场景 B 的成功衡量 | "答案准确率 ≥80% + 来源可追溯率 ≥90%（双指标，见下）" |
| 7 | **约束与边界** | 场景 B 的风险与应对 | "答案必须带来源引用，不能凭模型记忆瞎答；API key 走环境变量" |

> **第 5、6 项是最容易漏的，写不好 Agent 会卡住或走错路线：**
>
> - **数据现状**：如果你不写"政策文档 + Q&A 验证集"在哪、政策会更新，Agent 可能用模型记忆直接回答（那就不是 RAG 了），或者不知道测试集在哪。
> - **验收口径**：如果你不写"来源可追溯率"这个指标，Agent 可能只优化答案对错、不重视引用——可溯源是政务 AI 的命门。

### Step 2：组装启动提示词——先自己想，再看提示

> **不要急着看参考答案。** 写启动提示词的本质，是"**如果换一个人接手这个项目，他需要知道什么，才能不重开讨论、直接开干？**"

**先尝试自己组装。写的时候，用这 7 个问题自我审查：**

| 自查问题 | 为什么问这个 |
|---------|------------|
| ① 换一个不了解项目的人读，他能知道**我在为谁、做什么**吗？ | 缺了背景，Agent 会脑补客户和场景 |
| ② 他能不能知道**输入是什么、输出是什么**？ | 缺了 I/O，Agent 会发明错误接口 |
| ③ 他能不能知道**"为什么选 RAG 而不是微调/关键词搜索"**？ | 缺了选型理由，Agent 可能问你"要不要微调" |
| ④ 他能不能知道**数据是什么情况**（文档在哪、多少份、验证集在哪）？ | 缺了数据现状，Agent 可能凭记忆回答或找不到测试集 |
| ⑤ 他能不能知道**"做出来怎么算成功"**？ | 缺了验收口径（尤其"来源可追溯"），Agent 会自己发明一套 |
| ⑥ 他能不能知道**有什么红线**？ | 缺了约束，Agent 可能不重视来源引用 |
| ⑦ 他能不能知道**"按什么流程跑、产出什么"**？ | 缺了流程要求，Agent 不会主动走 gstack 八环节 |

> 如果你某一条答不上来——**回到实操一的方案框架场景 B 去找**。这轮你已经有实操二的经验，应该能更快写出来。

**✅ 自查通过后，检查点：**
- [ ] 你自己独立写出了启动提示词（先别翻附录）
- [ ] 7 个自查问题你都能回答"能"
- [ ] 数据现状（5 份政策文档 Lab3-policy-docs 目录 + Q&A 验证集）写进去了
- [ ] 验收口径（答案 ≥80% + 来源可追溯 ≥90%）写进去了
- [ ] 明确写了"按 gstack 八环节流程执行 + 每环节产出文件 + 需要拍板时停下问我"
- [ ] 你和组内对齐了这份提示词（Delta 主导，Echo 队友审查）

> 写完、组内对齐后，可以对照**附录 D：启动提示词参考答案**——看你自己写的，相比参考答案漏了什么、多了什么。

### Step 3：让 Agent 读取政策文档，附上 Q&A 验证集

在启动信息里，**指定政策文档的来源目录（`Lab3-policy-docs/`）和格式处理要求**，再附上 Q&A 验证集：

```
【政策文档】政策库在 Lab3-policy-docs/ 目录下，共 5 份文档，混合格式：
社保参保政策.md（Markdown）、医保报销政策.md（Markdown）、
保障性住房政策.md（Markdown）、户籍迁移政策.docx（Word）、
就业创业补贴政策.pdf（PDF）。
施工前请读取全部 5 份，对 .docx 和 .pdf 先做格式解析（转成可检索文本），
内容不得自行改动或编造。施工时用它作为知识库来源。

【Q&A 验证集】请写入 tests/qa_pairs.csv（含表头 question,expected_answer,expected_source）：
question,expected_answer,expected_source
外地户口在西岭稳定就业能交社保吗,可以，用人单位应当依法为其办理参保,社保参保政策.md 第十五条
外地户口灵活就业能在西岭交社保吗,可以，持居住证的灵活就业人员可参加社保,社保参保政策.md 第十六条
住院报销比例是多少,在职职工一级医院90%二级85%三级80%,医保报销政策.md 第五条
公租房申请条件是什么,本市户籍或持居住证满2年且人均住房面积低于15平方米,保障性住房政策.md 第五条
户口迁入西岭需要什么条件,有合法稳定住所或连续缴纳社保满3年或人才引进,户籍迁移政策.md 第五条
失业金能领多久,最长24个月,就业创业补贴政策.md 第七条
```

> **注意：**
> 1. 5 份文档是混合格式，`.docx` 和 `.pdf` 需要先解析成可检索文本再分块向量化。
> 2. **验证集的 expected_source 统一用转换后的 `.md` 文件名**（docx/pdf 解析后也是 .md），这样验收时系统引用能与验证集严格对上。
> 3. **验证集问题不要设计得太宽**：一个宽问题往往覆盖多个条款（如"外地户口能交社保吗"同时涉及稳定就业和灵活就业），单条款锚定必然歧义。拆细（稳定就业→第15条、灵活就业→第16条）能同时提升验收稳定性和教学清晰度。

---

## 2. 注入并跟进八环节（75 分钟）

### Step 1：把启动提示词灌进 office-hours

和实操二一样，用驱动指令把流程纪律"焊"进 Agent 的执行。参考驱动指令：

```
/office-hours

我是一名 FDE 团队（Delta 角色），需要启动西岭市民服务平台场景 B 的施工。

【在这里粘贴你写好的启动提示词，包括：背景 / 要做什么 / 技术选型 /
数据现状 / 验收口径 / 约束】

请以 gstack 标准开发流程执行本次开发，严格遵守以下要求：
1. 依次走完 8 个环节：office-hours → spec → autoplan → build →
   review → qa → ship → retro，顺序不可跳过、不可打乱。
2. 每个环节都要产出对应的文件（office-hours 记录 / SPEC.md /
   决策记录 / 代码 / review.md / qa-report.md / 版本与 CHANGELOG /
   retro.md），文件落到当前目录。
3. 走完一个环节、进入下一个之前，先停下来向我确认，等我确认后再继续。
4. 在需要我拍板的决策点（如分块大小、检索数量、兜底策略），
   明确列出选项和利弊，等我选择后再继续。
5. 现在从 office-hours 开始。
```

### Step 2：跟进八个环节，你的把关点

| 环节 | Agent 会做什么 | 你的 checkpoint（必须把关） |
|------|--------------|--------------------------|
| **office-hours** | 追问需求细节，尤其数据、口径、溯源 | 回答它关于文档/口径的问题；确认"值得做" |
| **spec** | 死磕边界：失败模式、No-Go、数据模型、验收口径 | 审查 No-Go 是否具体；**"答案必须带来源"是否写进 SPEC** |
| **autoplan** | 暴露两可决策，等你拍板 | **真的拍板**：切块策略、检索 top-K、检索不到怎么办 |
| **施工** | 按 SPEC 实现（文档→分块→向量化→检索→生成），每模块冒烟 | 抽查代码没偏离 spec；**政策文档来自 Lab3-policy-docs、不是 Agent 编的；5 份都读全了（含 docx/pdf）；建索引后对每条验证集问题抽查 top-k，目标条款在不在前几** |
| **review** | 两遍检查（致命+信息），P0-P3 分级 | 确认 P0/P1 修复；API key 没硬编码；**embedding 调用无硬编码模型名** |
| **qa** | 真实跑核心流程 + 边界输入 | 用 Q&A 验证集测；答案准确率 + 来源可追溯率双指标达标 |
| **ship** | 版本+CHANGELOG+README+commit | 确认 .env 未入库 |
| **retro** | 复盘四点 | 摩擦写具体；有改进行动项 |

### 关键决策点，Agent 停下来时你要拍板

实操三有几个**实操二没有的决策点**（都围绕 RAG 的特性）：

1. **切块策略**：政策文档天然按「条」组织，**推荐按条切块**（正则匹配行首的"第X条"，每条一个块，条款号随块保留）——引用粒度天然对齐"第X条"，与验证集的 expected_source 零转换。固定长度切块（如 800 字符）会一次塞进好几条政策、切断条款号，不推荐。**同时注意：政策文档末尾的"本政策由XX负责解释 / 自XX起施行 / 有效期"这类程序性条款，对所有问题都呈中等相似度、互相高度相似，会污染检索——建索引时建议过滤**（正则匹配"负责解释/施行/有效期"）。
2. **检索数量（top-K）**：检索几条片段喂给模型？太少答案不全，太多噪声大。实测 top-5 配合程序性条款过滤，效果稳定。
3. **检索不到时怎么办**：政策库里没有这个问题，模型该怎么答？**绝不能编**——应明确说"政策库中未找到，建议咨询人工"。注意：相似度阈值只是"进生成的门槛"，检索到相似度不高的片段进了生成时，**拒答靠提示词约束模型判断兜底**——这是两层防线，验收时要各测一条（准备 1-2 条库外问题实测拒答）。
4. **来源引用格式**：答案引用要不要显示"文件名 + 条款号"？要——这是政务可溯源的底线。
5. **建索引后必须抽查检索**：建完索引，对每条验证集问题输出 top-k 排名，人工扫一眼目标条款在不在前几。**不抽查直接验收，目标条款可能被程序性条款挤掉，首轮必 FAIL、白花几十分钟**——这是 RAG 施工最实用的经验。

> **FDE 灵魂追问 #1：** Agent 在 autoplan 停下来问"检索不到时模型怎么回答"——你不能说"你看着办"。为什么？因为**政务场景，编造政策答案是事故**（坐席/平台要负责）。你必须拍板"检索不到必须明说，不能编"。这个决策 AI 不知道严重性，只有你知道。

---

## 3. 验收与收尾（20 分钟）

### Step 1：核对交付物

```
实操三-政策问答/
├── 01-office-hours.md
├── SPEC.md                   # 含失败模式 + No-Go + 数据模型 + 验收口径
├── SPEC-决策记录.md           # autoplan 拍板记录
├── docs/                     # 政策库（从 Lab3-policy-docs/ 读取，或复制到此）
├── src/                      # 分块/向量化/检索/生成模块
├── streamlit_app.py          # 对话界面（命名自定，功能一致即可）
├── tests/qa_pairs.csv        # Q&A 验证集
├── review.md / qa-report.md
├── VERSION / CHANGELOG.md / README.md
└── retro.md
```

### Step 2：验收口径——答案 + 来源双指标

RAG 的验收比分类器复杂，用**双指标**：

| 指标 | 要求 | 判定机制（怎么算"对"） |
|------|------|------------------------|
| **答案准确率** | ≥80% | 让 LLM 做语义评判：对照 expected_answer 判"一致/不一致"（输出结构化结果），比关键词匹配鲁棒 |
| **来源可追溯率** | ≥90% | 严格比对：回答引用的"文件名 + 条款号"与 expected_source 一致才算对（条款号归一化：'第16条'/'第十六条'都算 16；漏引/错引/不符都算未达标） |

> **为什么"答案对但来源错"也要扣分？** 因为政务场景的命门是可溯源。**答案对了、来源引错了，等于让市民去核对一个不存在的文件——比不答更糟。** 所以"来源可追溯"单列一个 90% 的硬指标。

> **统计提示：** 6 条验证集上，"可追溯率 ≥90%" 实际意味着最多只能错 1 条（5/6≈83% 就挂了）。"答案 ≥80%" 同理（5/6 才达标）。验收时对这个"差一条就不过"的线要有心理预期，避免差一条时困惑。

### Step 3：演示给"坐席组长小王"看

模拟向小王演示这个系统。要点：
- 用**她听得懂的话**讲（别说"RAG""向量检索"）
- 演示一个"带来源引用"的回答——"系统答完还告诉市民是哪份文件、哪一条，你可以去核对"
- 呼应她的"答错政策担责任"的顾虑——"以后政策答错了，责任能追溯到文件，不用你背锅"

> **FDE 灵魂追问 #2：** 向小王演示时，为什么"来源引用"是最好的切入点？——因为她最怕"答错政策被追责"。**来源引用把"AI 答错了谁负责"这个问题，从"坐席背锅"变成了"按文件来，有据可查"。** 这正是场景 B 的核心价值，也是 RAG 在政务场景不可替代的原因。

### Step 4：DoD 验收（演示级声明）

在 qa-report 或 retro 里，写一句**验收定位声明**：

> "本项目使用 5 份政策文档 + 6 条 Q&A 验证集，属**演示级验收**——结论仅证明方案在给定政策库和样本上可行，不宣称覆盖全部政策或达到生产可用。"

> **FDE 灵魂追问 #3：** 为什么必须写这句"演示级验收声明"？——**FDE 的第一戒律是不能夸大交付物。** 5 份政策答得好，不代表市民问的所有政策都能答。而且政务场景夸大更危险——**说"覆盖全部政策"而实际没有，市民按错误信息办事，责任是平台扛。** 敢写"答案准确率 100%"吗？

---

## 4. 复盘沉淀（10 分钟）

### Step 1：看 Agent 的 retro 产出

Agent 跑 retro 时会产出复盘四点（做得好/摩擦/未覆盖/行动项）。

### Step 2：手动补充"对下一轮有用"的两条

在 retro.md 追加：

1. **AGENTS.md 更新**：让 Agent 更新项目根目录的 AGENTS.md（政策文档结构、向量库路径、Q&A 验证命令、来源 metadata 口径）——实操四（场景 C）与 Lab4B 开新会话时，AI 能继承上下文。
2. **改进行动项交接**：写下"下一轮（场景 C 工单分级）开工前，我要先看这条经验：____"
3. **面向 Lab4B 的最小政策工具契约（长期交接）**：如果其他系统调用本次 RAG（而不是由用户直接操作页面），调用方至少需要哪些字段？建议参考接口（只记录、不要求服务化改造）：

```text
search_policy
输入：question
输出：answer、evidence、evidence_sufficient、status
```

> **来源 metadata 统一口径（写入 retro，Lab4B 沿用）：**
> - `source_file`：原始文件名，如 `户籍迁移政策.docx`
> - `normalized_file`：转换后的检索文件名，如 `户籍迁移政策.md`
> - `article`：条款号
> - `excerpt`：引用片段
>
> 面向市民和坐席展示 `source_file + article`；内部索引、调试和回验用 `normalized_file`；验收脚本按明确的 metadata 字段比较，不依赖展示文本中的偶然格式。

> **关于检索方案的说明（为 Lab4B 铺垫）：** 实操三使用 Chroma + embedding，是为了完整体验 RAG 主链路。Lab4B 为降低综合实验复杂度，可以采用 BM25 + jieba 或其他经拍板的轻量检索方案。两者必须共同守住"没有证据不回答、来源可以核对"的底线。

> **衔接实操四：** 实操四开工时，新会话读实操三的 AGENTS.md + retro.md，继承"这个项目怎么跑、有哪些坑"的上下文。**本轮的政策文档、来源 metadata 和无证据兜底，将在 Lab4B 中作为 `search_policy` 工具的契约基础。**

---

## 5. 完成标准（DoD）

- [ ] RAG 依赖已装好（向量库 + embedding），验证可用
- [ ] 写了高质量的 office-hours 启动提示词（含数据现状 + 双指标验收口径 + 政策文档 + Q&A 验证集）
- [ ] Agent 按 gstack 八环节自动跑完，每环节有产出文件
- [ ] 每次拍板都真的做了决策（分块大小、检索数量、检索不到兜底）
- [ ] 验收口径写进 SPEC：答案 ≥80% + 来源可追溯 ≥90%
- [ ] 政策问答系统可运行，双指标达标
- [ ] 5 份政策文档来自 Lab3-policy-docs（不是 Agent 编的），且 docx/pdf 已正确解析（转换脚本留档，政策更新后可复现重转）
- [ ] .env 未入库，P0/P1 已修复
- [ ] 向小王演示成功（讲了"来源引用 = 追责有据"）
- [ ] 写了"演示级验收声明"
- [ ] retro.md 追加了 AGENTS.md 更新 + 交接行动项
- [ ] 3 个 FDE 灵魂追问参与讨论
- [ ] **已记录 `search_policy` 的最小契约，并统一原始文件、转换文件、条款和摘录的来源字段（source_file / normalized_file / article / excerpt）**

---

## 附录 A：gstack 安装速查

| 方式 | 操作 |
|------|------|
| Agent 自装 | 用第 0 节的提示词让 opencode 装 |
| 手动 | `git clone https://github.com/garrytan/gstack.git ~/gstack && cd ~/gstack && ./setup --host opencode` |
| 验证 | `~/.config/opencode/skills/` 下应有 office-hours/spec/autoplan/review/qa/ship/retro 等目录 |

## 附录 B：FDE 灵魂追问汇总

| # | 环节 | 追问 |
|---|------|------|
| 1 | 注入跟进 | "检索不到"拍板为什么必须是人的？编造政策答案的后果？ |
| 2 | 验收收尾 | 为什么"来源引用"是最好的向小王演示切入点？ |
| 3 | DoD | 为什么必须写"演示级验收声明"？政务场景夸大更危险在哪？ |

## 附录 C：讲师引导要点

- **Step 1 技术选型是本轮教学核心**：观察学员是否说清"为什么 RAG 不用微调/关键词搜索"。政策更新+需溯源+防幻觉是三个关键理由。
- **数据现状**：最容易漏的是"政策会更新，需重建索引"——漏了 Agent 可能不做索引更新机制。
- **双指标验收**：学员容易只盯"答案对错"，忽视"来源可追溯"。讲师要点醒——政务场景来源错误比答案错误更糟。
- **决策点（切块策略/top-K/检索不到兜底）**：观察学员是否真拍板，还是"你看着办"。检索不到必须明说不能编，这是政务底线。
- **验证集设计（本轮核心教学点）**：宽问题 + 单条款锚定 = 歧义。引导学员把宽问题拆成多条（如"外地户口能交社保吗"拆成稳定就业/灵活就业两条），既提升验收稳定性，也是 RAG 特有的设计判断力。
- **衔接实操二**：实操二 retro 的改进行动项，实操三开工前有没有先看？Agent 复用 AGENTS.md 了吗？

## 附录 D：启动提示词参考答案

> **⚠️ 仅供环节 1 完成后对照，不要先看。**

```
请以 FDE 团队的 Delta 角色，为西岭市民服务平台启动场景 B 的施工。

【背景】我们上一轮完成了场景 A（诉求分类），陈主任催着推进下一个子场景。场景 B
（政务政策问答）已选型为 RAG，现在由 Delta 施工。

【要做什么】做一个政务政策知识问答系统：市民/坐席输入政策问题，系统从政策文档
检索并生成带来源引用的精确回答。

【输入输出】输入：一个政策问题（如"外地户口能在西岭交社保吗"）。
输出：回答 + 来源引用（哪份政策文件、哪一条）。

【技术选型】RAG。不用微调（政策常更新、需溯源、易幻觉）、不用纯关键词搜索
（自然语言和文件表述有语义鸿沟）。

【数据现状】政策库在 Lab3-policy-docs/ 目录（5 份政策文档，混合格式，见下方
【政策文档】）+ Q&A 验证集（落盘 tests/qa_pairs.csv）。政策会更新，需要索引重建机制。

【验收口径】双指标：答案准确率 ≥80% + 来源可追溯率 ≥90%。
来源错误（引用错文件/条款）算未达标，即使答案正确。

【约束】答案必须带来源引用，检索不到时明确说"政策库中未找到，建议咨询人工"，
绝不能编造；API key 走环境变量不硬编码。

【政策文档】政策库在 Lab3-policy-docs/ 目录，共 5 份文档，混合格式：
社保参保政策.md、医保报销政策.md、保障性住房政策.md（Markdown）、
户籍迁移政策.docx（Word）、就业创业补贴政策.pdf（PDF）。
施工前请读取全部 5 份，对 .docx 和 .pdf 先做格式解析（转成可检索文本），
内容不得自行改动或编造。

【Q&A 验证集】请把以下数据写入 tests/qa_pairs.csv（表头 question,expected_answer,expected_source）：
（6 条 Q&A，见环节 1 Step 3）

请按 gstack 标准开发流程（office-hours → spec → autoplan → build →
   review → qa → ship → retro）执行这次开发，每一环节产出文件，并在我需要拍板的地方停下来问我。
```

**对照要点：**
- **必备信息块**：背景、要做什么、输入输出、技术选型（含理由）、数据现状、**政策文档 + Q&A 验证集**、验收口径（双指标）、约束。
- **三处"防翻车"信息**：①"不用微调/关键词搜索"（防 Agent 走错路线）；②"来源错误算未达标"（防只优化答案不重视引用）；③"检索不到必须明说不能编"（防编造政策，政务底线）。
- **流程要求**：末尾提"按流程执行"，真正焊死靠驱动指令。

---

## 附录 E：技术选型背景与教科书对齐说明

> 本附录回答三个问题：**① 为什么这么选？② 相对教科书简化了什么、为什么？③ 想对齐教科书的高级场景，技术条件和提示词怎么改？** 供学有余力的学员扩展，也供讲师备课引用。

### E.1 技术选型：为什么用这些

本实操的 RAG 技术栈：**Chroma（向量库）+ bge-m3（embedding，走 API）+ DeepSeek（生成）**。选型逻辑如下：

**向量数据库为什么用 Chroma？**

| 维度 | Chroma（本实操） | 教科书第 9 章 9.6.2 的其他选项 |
|------|-----------------|------------------------------|
| 定位 | 轻量开发向量库 | Milvus（大规模生产）/ pgvector（已有 PG） |
| 部署 | `pip install` 一行 | Milvus 需 K8s 运维，pgvector 需 PG 基础设施 |
| 适用规模 | < 100 万向量 | Milvus 数十亿 / pgvector < 100 万 |
| 推荐场景 | **PoC / MVP** | 生产 / 大规模 |

教科书（第 9 章 9.6.3）的 FDE 决策逻辑说得清楚：**"你在搭 PoC → 用 Chroma。pip install 一行，4 个核心 API，十分钟跑通全流程。"** 本实操就是 PoC——用最简单的方式跑通完整的 RAG 管线，验证"政策问答"这个场景可行。**不为 100 份文档搭 Milvus 集群**，是 FDE 的基本判断。

**Embedding 为什么用 bge-m3（走 API）？**

- 教科书（第 9 章 9.5.2）：BGE-M3 是**中文 RAG 的事实标准**——1024 维、支持 8192 token、三重检索能力（Dense 语义 + Sparse 关键词 + ColBERT 精细匹配）、月下载量 3196 万。
- 本实操**走 API 而非本地跑**（见课前准备）：实操二已确认本项目数据可出域，且本地跑要下载 2GB+ 模型 + torch，机器配置低会卡死。API 方式免运维、快速验证。
- 生产替代：预算允许且精度要求高时，教科书建议升级 Qwen3-Embedding-8B（MTEB 多语言第一）。

**为什么是 DeepSeek 做生成？** 教科书第 9 章 9.8 明确用它；DeepSeek 在 RAG 场景幻觉低、能可靠引用文档内容，且是国产开源模型、契合政企数据约束。

### E.2 相对教科书的简化：做了什么、为什么

教科书第 9 章是完整的企业级 RAG 工程课，本实操是 **120 分钟的 PoC**，为了在时间内跑通，做了以下简化：

| 教科书完整方案（第 9 章） | 本实操（简化后） | 简化的原因 |
|----------------------|----------------|-----------|
| **文档解析**：Docling 等专业解析器，处理扫描件/复杂排版（6.3） | 简单转文本（docx/pdf 用脚本转 md） | 政策文档是结构化文本，无需 OCR；聚焦 RAG 主链路 |
| **分块**：512 tokens + 20% 重叠，按语义切（6.4） | 按「条」切块（每条一块，不设固定长度） | 政策文档天然按条组织，按条切块引用粒度对齐验证集 |
| **检索**：混合检索（Dense+Sparse）→ 重排序到 top-10（6.6） | 仅 Dense 余弦相似度 + top-k（本实操 top-5） | 政策条款语义清晰、问题规范，基础检索够用；混合检索/重排序是"发现问题再升级" |
| **评估**：RAGAS 三维（忠实度/上下文精度/回答相关性）（6.7） | 双指标（答案准确率 + 来源可追溯率） | RAGAS 需要专门工具，双指标已覆盖核心（答得对 + 有据可查）；RAGAS 是生产级评估 |
| **生产化**：访问控制、增量更新、持续幻觉监控（6.7） | 演示级验收声明 + 政策更新需重建索引 | PoC 阶段不搭生产设施；"演示级"声明避免夸大 |

> **简化不是偷懒，是 FDE 的"从最简单开始"原则。** 教科书第 9 章 9.7.2 说：先用基础检索跑通，**如果评估发现某一类查询持续表现差——再针对性地加 HyDE 或重排序，而不是堆砌技术**。本实操的教学目标就是让你先跑通基础链路，理解 RAG 各环节的作用，为将来按需升级打底。

### E.3 对齐教科书高级场景：技术条件与提示词

如果学员想把本实操升级到教科书第 9 章的完整方案，可按下面配置：

**1. 检索质量升级（第 9 章 9.7.2）——混合检索 + 重排序**

- **技术条件**：
  - bge-m3 本身支持 Dense + Sparse 双向量，用 `hybrid_search=True` 启用混合检索（硅基流动/ChromaLlamaIndex 均支持）
  - 重排序需 bge-reranker-v2-m3（教科书实测能提升 top-5 召回率 10-20%）
- **启动提示词补一句**：
  ```
  检索环节请启用混合检索（Dense 语义 + Sparse 关键词融合），
  检索到 top-100 后用 bge-reranker-v2-m3 重排序到 top-10，
  再喂给生成模型。
  ```

**2. 评估升级（第 9 章 9.9.2）——RAGAS 三维**

- **技术条件**：安装 `ragas`，用 RAGAS 的 Faithfulness / Context Precision / Response Relevancy 三指标替代/补充双指标。
- **启动提示词补一句**：
  ```
  评估环节请用 RAGAS 框架：忠实度（回答的每个声明能否在检索文档中找到依据）、
  上下文精度（相关 chunk 是否排在前面）、回答相关性（回答是否切题）。
  验收标准：三指标均 ≥0.7。
  ```

**3. 生产化（第 9 章 9.9.4）——访问控制 + 增量更新**

- **技术条件**：索引时为每条 chunk 存 ACL 元数据（哪些角色可见）；文档更新时删除旧 chunk、插入新 chunk，而非全量重建（Chroma 支持元数据过滤，LlamaIndex 支持增量索引）。
- **启动提示词补一句**：
  ```
  索引入库时，为每条 chunk 附加来源文档和可访问角色的元数据；
  政策更新时采用增量更新（删除旧 chunk、插入新 chunk），
  并保留 ACL 过滤能力。
  ```

**4. 查询优化（第 9 章 9.7.2）——HyDE / 查询改写**

- **技术条件**：查询环节多一次 LLM 调用生成假设回答（HyDE）或改写口语化查询。
- **启动提示词补一句**：
  ```
  对短查询和口语化查询，先用 LLM 做查询改写（或生成假设文档 HyDE），
  再用改写后的查询做检索。
  ```

> **升级原则提醒（教科书第 9 章 9.7.2 原话的延伸）：** 不要一次性把上面全加上——那是堆砌技术。正确做法是：先跑通基础版，用评估找出"哪一类查询表现差"，**针对性地只加对应那一项**。FDE 的功夫在"什么时候不加复杂度"，而不是"能加多少"。
