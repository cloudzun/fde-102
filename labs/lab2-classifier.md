# 实操二：Delta 施工——市民诉求智能分类系统（场景 A）

> **对应课纲：** FDE 综合实操 · 实操二（Delta 视角 · 场景 A）
> **时长：** 约 120 分钟
> **角色：** 你切换为 **Delta（FDE/FDSE）**——手上沾泥，把方案变成能跑的代码
> **工作流：** gstack 八环节，一次注入启动，checkpoint 把关
> **前置：** 已完成实操一，组内拥有《解决方案框架》

---

## 实操概述

上一轮，你的组作为 **Echo**，把西岭市民服务平台的"整套智能化"拆成了三个子场景，并选好了技术路线——其中**场景 A：市民诉求智能分类**被定为快赢、第一个施工。

现在你们**切换成 Delta 角色**，要把它真正做出来。

> **你的施工任务书**就是实操一产出的《解决方案框架》。这一轮，你不再是"想清楚做什么"的人，而是"把它做出来"的人。

**目标**：用 AI（opencode）在 2 小时内，做出一个**市民诉求五分类系统**——输入一句市民诉求，输出它该转给哪个类别（住建 / 人社 / 市监 / 城管 / 其他），按约定的验收口径达标，并带一个 Streamlit 网页验证面板。

**技术方案不变**（来自实操一选型）：
- **文本分类**（不是 RAG、不是 Agent）——输入一句诉求、输出一个类别，一次调用搞定。
- 技术栈：opencode + gstack 工程流程 + DeepSeek + Streamlit。

> **本实验的能力定位：** 它只解决"**这是什么**"——输入一条诉求，输出业务类别和不确定性判断（是否建议转人工）。在后续的附加高级实验 Lab4B 中，分类会被视为**可选辅助能力**，而不是每条请求的强制第一步——明确政策问题可以直接查政策，明确部门问题可以直接查职责，只有类别本身有助于后续判断时才值得调用分类。Lab4B 不会强制复用本实验的代码，本实验的重点是把"分类"这块能力积木做小、做清楚、做可验证。

---

## 本轮的核心学习点：怎么让 Agent 替你跑流程

这一轮和上一轮（Echo 头脑风暴）最大的区别是——**你不再手动驱动每一步，而是把"启动信息"一次交给 Agent，让它照着 gstack 标准流程自己跑，你在每个 checkpoint 把关。**

这像极了真实 FDE 的工作方式：你不需要手把手教 AI 写每一行代码，**你要做的是——把客户需求翻译成一段高质量的启动提示词，然后在 AI 干活的关键节点做判断和决策。**

```
Echo（上轮）：人脑挖需求 → 方案框架
                ↓ 你来做这段"翻译"
Delta（本轮）：把方案框架 → office-hours 启动提示词
                ↓ Agent 自动跑 8 环节
                ↓ 你在每个 checkpoint 把关
          可运行的分类系统
```

> **这就是 FDE 项目中"把用户需求细化、映射成实现思路"的过程。** 方案框架是"客户的语言"，启动提示词是"AI 的语言"，你站在中间把它翻译好。

---

## 知识背景：gstack 是什么

**gstack** 是 Garry Tan（YC CEO）开源的 AI 开发工作流框架，以 skill 形式安装在 opencode 中，用 `/命令` 调用。它把一次完整的 AI 开发迭代组织为**八个环节的流水线**，上一环的产出是下一环的输入，**没有环节可以被跳过**：

```mermaid
graph LR
    OH["office-hours<br/>要不要做"] --> SP["spec<br/>死磕边界"]
    SP --> AP["autoplan<br/>决策拍板"]
    AP --> BL["build<br/>做出来"]
    BL --> RV["review<br/>找隐患"]
    RV --> QA["qa<br/>测体验"]
    QA --> SH["ship<br/>收尾"]
    SH --> RT["retro<br/>复盘"]

    style OH fill:#4C78A8,color:#fff
    style SP fill:#72B7B2,color:#fff
    style AP fill:#F58518,color:#fff
    style BL fill:#E45756,color:#fff
    style RV fill:#B279A2,color:#fff
    style QA fill:#54A24B,color:#fff
    style SH fill:#B279A2,color:#fff
    style RT fill:#FF9DA6,color:#333
```

文字版流水线：

```
office-hours → spec → autoplan → build → review → qa → ship → retro
（要不要做） （死磕边界） （决策拍板） （做出来） （找隐患） （测体验） （收尾） （复盘）
```

**八个环节各是什么：**

| 环节 | 命令 | 干什么 | 你的角色 |
|------|------|--------|---------|
| **office-hours** | `/office-hours` | 想清楚该不该做，确认需求和范围 | 提问者 |
| **spec** | `/spec` | 把需求死磕成精确规格——追问失败模式、硬边界（No-Go）、数据模型、验收口径 | 追问者 |
| **autoplan** | `/autoplan` | 把"两可决策"暴露出来，供人拍板（它只列选项，不替你做决定） | **决策者** |
| **施工** | 自然语言 | 按 SPEC 实现，每模块即测 | 审查者 |
| **review** | `/review` | 找"能跑但会出事"的隐患：分两遍（致命检查 + 信息检查），发现按 P0-P3 分级 | 审查者 |
| **qa** | `/qa` | 用真实浏览器跑核心流程 + 边界输入 | 验收者 |
| **ship** | `/ship` | 版本号 + CHANGELOG + README + commit，规范化收尾 | 收尾者 |
| **retro** | `/retro` | 复盘：做得好 / 摩擦 / 未覆盖测试 / 改进行动项 | 复盘者 |

**几个关键原则：**

- **人的角色每环节都不同**：office-hours 你是提问者，spec 你是追问者，autoplan 你是决策者，review 你是审查者——但**任何环节，人的判断都不可替代**。
- **每一环的产出都是文件**（Markdown），不是口头结论——团队协作可追溯、可复用。
- **不是所有改动都要走全流程**——部署配置、文案调整这类操作性工作，直接提示词驱动即可。判断标准：做错代价大不大、会不会被别人用、会不会长期存在。本轮做分类系统会被坐席长期使用 → **走完整流程**。
- **本轮最关键的升级**：你和 Agent 的分工不再是"你一步步教它"，而是"**你把启动信息写好，它照流程自己跑，你在 checkpoint 把关**"。

---

## 0. 课前准备

- [ ] opencode Desktop 已安装并启动
- [ ] 已通读实操一的《解决方案框架》——特别是场景 A 的选型、验收、优先级结论
- [ ] DeepSeek 官方 API key 已配置（`DEEPSEEK_API_KEY`，在 `api.deepseek.com` 获取）
- [ ] Python 3.11+ 已安装
- [ ] 创建本实操目录 `实操二-诉求分类/`

### 测试样本（写在启动提示词里，让 Agent 落盘）

> **测试数据直接写在环节 1 的启动提示词里（见下），Agent 会在施工前把它写成 `tests/test_cases.csv`。不要让 Agent 自己编数据。**

- **为什么把数据放在启动提示词里**：测试集必须是"外部给定的"——由本手册提供固定数据，而不是让 Agent 自己出题自己考。把数据写进启动提示词，既保证了数据固定，又免去手动管理文件的麻烦。
- **落盘路径**：Agent 收到数据后，会写成 `实操二-诉求分类/tests/test_cases.csv`

> **衔接实操一：** 打开你们的《解决方案框架》，找到场景 A——"诉求智能分类：把市民诉求自动分到对应委办局"。这轮就是照着它施工。

### 安装 gstack（两条路任选）

**路线 A：让 Agent 自己装（推荐）**

在 opencode 中，把这个提示词交给 Agent，它会帮你装好：
```
请帮我安装 gstack（Garry Tan 开源的 AI 开发工作流框架）：
1. 克隆 https://github.com/garrytan/gstack.git 到用户主目录
2. 运行它的 setup 脚本，host 选 opencode
3. 装完后验证一下 /office-hours /spec /autoplan /review /qa /ship /retro 这些技能
   都出现在 ~/.config/opencode/skills/ 下
4. 告诉我安装结果
```

**路线 B：手动安装**

```bash
git clone https://github.com/garrytan/gstack.git ~/gstack
cd ~/gstack && ./setup --host opencode
```

> **衔接实操一：** 打开你们的《解决方案框架》，找到场景 A——"诉求智能分类：把市民诉求自动分到对应委办局"。这轮就是照着它施工。

---

## 1. 写启动提示词（20 分钟）★本轮核心

> **这不是让 AI 干的活，是你（Delta）的活。** 你要把实操一的方案框架，翻译成一段能让 Agent 开跑的 office-hours 启动信息。写得好不好，直接决定 AI 能不能按标准流程跑对。

### Step 1：先理清"要交给 Agent 哪些信息"

对照实操一的方案框架，把场景 A 的相关结论摘出来，组装成一份完整的启动信息。一份好的启动信息要包含这 7 项，**每项都给出确定的内容，不是留白让 Agent 猜**：

| # | 要包含的信息 | 从方案框架哪里找 | 启动信息里怎么写 |
|---|------------|----------------|----------------|
| 1 | **背景与角色** | 你是谁、在给谁做 | "我是 FDE 团队的 Delta，为西岭市民服务平台施工场景 A" |
| 2 | **要做什么** | 场景 A 定义 | "做一个市民诉求五分类系统" |
| 3 | **输入输出** | 场景 A 的"解决什么" | "输入一句诉求文本，输出类别：住建/人社/市监/城管/其他" |
| 4 | **技术选型（含理由）** | 场景 A 选型 | "已选型文本分类，不用 RAG/Agent，因为一次调用即可" |
| 5 | **数据现状 + 测试集** | 场景 A 的数据风险 | "无历史标注数据 → 必须走大模型零样本分类，不能用传统机器学习。测试集数据见下，请写入 tests/test_cases.csv" |
| 6 | **验收口径** | 场景 A 的成功衡量 | "自信区准确率 ≥85%，转人工比例 ≤30%，转人工不算错" |
| 7 | **约束与边界** | 场景 A 的风险与应对 | "API key 走环境变量不硬编码；不做 40+ 部门细分类" |

> **第 5、6 项是最容易漏的，写不好 Agent 会卡住或走错路线：**
>
> - **数据现状**：如果你不写"没有历史标注数据"，Agent 可能默认走传统机器学习（TF-IDF + 分类器需要上千条标注），那是死路。必须明确写"→ 走大模型零样本分类"。
> - **验收口径**：如果你不写"转人工不算错"，Agent 可能做成"整体准确率"但把转人工的样本算成错——这跟"拿不准转人工"的设计自相矛盾。必须明确写"自信区 ≥85% + 转人工 ≤30%，转人工不算错"。

**启动提示词里必须附上测试数据本身（让 Agent 落盘成 csv）：** 在启动信息的"数据现状"之后，粘贴下面这份数据，并写明"请把这些数据写成 `tests/test_cases.csv`，施工时用它作为测试集"。数据是固定提供的，不能让 Agent 自己生成。

```
以下是测试集数据，请写入 tests/test_cases.csv（包含表头 id,text,label）：
id,text,label
1,我家门口的路灯坏了两个星期了，晚上一片漆黑，一直没人来修,住建
2,小区楼道的墙皮一直渗水，物业说管不了，让我自己找开发商,住建
3,开发商交房三年了，房产证还迟迟办不下来，找了好几次都没结果,住建
4,新买的房子天花板有裂缝，开发商只答应刷漆，想咨询能不能要求退房,住建
5,小区电梯频繁故障，物业说修不好，担心老人孩子坐电梯出事,住建
6,我是外地户口，想问问能不能在西岭市自己交社保,人社
7,公司连续三个月没给我交医保，现在离职了，想知道怎么自己续交,人社
8,失业金领取需要准备哪些材料？我下个月就离职了,人社
9,退休金怎么办理领取手续？我妈退休三个月了还没领到钱,人社
10,想申请创业担保贷款，需要满足什么条件，能补贴多少利息,人社
11,在超市买到过期食品，商家不承认是他们的问题,市监
12,美容院办卡后跑路了，我的预付卡还能要回钱吗,市监
13,网购的电器坏了，商家说是人为损坏拒绝保修,市监
14,健身房突然关门，会员卡还有两年有效期，想退钱,市监
15,菜市场有摊贩卖的猪肉好像没有检疫标志，该向哪里举报,市监
16,楼下烧烤店占道经营，晚上油烟和噪音吵得睡不着,城管
17,有人在小区绿地上私搭违建，已经举报过三次都没处理,城管
18,小区门口有人乱停共享单车，堵住了消防通道,城管
19,施工工地夜间施工扰民，城管管不管,城管
20,路边摊贩把污水直接倒在人行道上，又脏又滑,城管
21,想咨询一下市图书馆周末的开放时间是几点,其他
22,政务大厅下午几点下班？办理身份证要预约吗,其他
23,我家的狗丢了，想问问宠物走失该去哪里登记寻找,其他
24,想了解一下市民卡丢了怎么补办，需要哪些证件,其他
25,请问社区免费体检什么时间开始，需要带什么材料,其他
```

> **注意：** 这份数据是"外部给定"的测试集，粘贴时**原样照抄，不要改、不要让 Agent 重新造一份**。它的作用是固定验收口径——25 条、每类 5 条、答案确定，Agent 才能公平地被检验。

### Step 2：组装启动提示词——先自己想，再看提示

> **不要急着看参考答案。** 写启动提示词的本质，是"**如果换一个人接手这个项目，他需要知道什么，才能不重开讨论、直接开干？**"——站在这个角度，你就能自己列出来。

**先尝试自己组装。写的时候，用这 7 个问题自我审查：**

| 自查问题 | 为什么问这个 |
|---------|------------|
| 1. 换一个完全不了解项目的人来读，他能知道**我在为谁、做什么**吗？ | 缺了背景，Agent 会自己脑补客户和场景 |
| 2. 他能不能知道**输入是什么、输出是什么**？ | 缺了 I/O，Agent 会发明错误的接口 |
| 3. 他能不能知道**"为什么选文本分类而不是 RAG/Agent"**？ | 缺了选型理由，Agent 可能又问你要不要用 RAG |
| 4. 他能不能知道**数据是什么情况**（多少条、标注没标注、测试集在哪）？ | 缺了数据现状，Agent 可能走传统 ML 死路，也可能找不到测试集 |
| 5. 他能不能知道**"做出来怎么算成功"**？ | 缺了验收口径，Agent 会自己发明一套验收 |
| 6. 他能不能知道**有什么红线**（不做的事、不碰的数据）？ | 缺了约束，Agent 会自由发挥 |
| 7. 他能不能知道**"按什么流程跑、产出什么"**？ | 缺了流程要求，Agent 不会主动走 gstack 八环节 |

> 如果你某一条答不上来——**回到实操一的方案框架去找**。场景 A 的定义、选型理由、成功衡量、风险应对，方案框架里都有。你写不出某一项，往往不是"不会写"，而是"方案框架那部分还没吃透"。

**✅ 自查通过后，检查点：**
- [ ] 你自己独立写出了启动提示词（先别翻附录）
- [ ] 7 个自查问题你都能回答"能"
- [ ] 数据现状（无历史标注 → 零样本）写进去了
- [ ] 测试数据（25 条，原样照抄）附在启动提示词里，并写明写入 `tests/test_cases.csv`
- [ ] 验收口径（自信区≥85% + 转人工≤30%）写进去了
- [ ] 明确写了"按 gstack 八环节流程执行 + 每环节产出文件 + 需要拍板时停下问我"
- [ ] 你和组内对齐了这份提示词（Delta 主导，Echo 队友审查是否忠实于方案框架）

> 写完、组内对齐后，可以翻开**附录 D：启动提示词参考答案**对照——看你自己写的那份，相比参考答案漏了什么、多了什么。漏的是"你没想到的"，多的是"你的理解"，都有价值。

---

## 2. 注入并跟进八环节（75 分钟）

### Step 1：把启动提示词灌进 office-hours

打开 opencode 的新对话，把启动提示词作为 **office-hours 的输入**注入。不是直接丢一段话，而是明确告诉 Agent"你要走什么流程"。参考驱动指令：

```
/office-hours

我是一名 FDE 团队（Delta 角色），需要启动西岭市民服务平台场景 A 的施工。

【在这里粘贴你写好的启动提示词，包括：背景 / 要做什么 / 技术选型 /
数据现状 / 验收口径 / 约束】

请以 gstack 标准开发流程执行本次开发，严格遵守以下要求：
1. 依次走完 8 个环节：office-hours → spec → autoplan → build →
   review → qa → ship → retro，顺序不可跳过、不可打乱。
2. 每个环节都要产出对应的文件（office-hours 记录 / SPEC.md /
   决策记录 / 代码 / review.md / qa-report.md / 版本与 CHANGELOG /
   retro.md），文件落到当前目录。
3. 走完一个环节、进入下一个之前，先停下来向我确认，等我确认后再继续。
4. 在需要我拍板的决策点（如验收口径、准确率门槛、兜底策略），
   明确列出选项和利弊，等我选择后再继续。
5. 现在从 office-hours 开始。
```

> **为什么要这样注入，而不是直接丢一句话？** 因为 Agent 默认只理解"你要什么"，不理解"你要它按什么纪律走"。这 5 条要求就是把 gstack 的流程纪律"焊"进它的执行里——**"走完一环停下来确认""拍板点列选项等你选"这两条尤其关键**，否则 Agent 可能一口气跑完，让你失去所有把关的机会。

> **FDE 灵魂追问 #0：** 为什么注入时要强调"走完一个环节就停下来等我确认"？——想想如果 Agent 一口气把 8 环节全跑完、代码也写完了才告诉你，你会怎样？**你失去了所有"中途纠偏"的机会**，只能事后全盘检查。真实 FDE 项目里，让 AI"一小步一确认"不是效率低，是**把失控风险控制住**。

**✅ 检查点：**
- [ ] 你的启动提示词作为输入，灌进了 `/office-hours`
- [ ] 明确要求"按 gstack 八环节执行，一环一停，拍板点列选项"
- [ ] 你和 Agent 的对话已经开了头，它在做 office-hours

### Step 2：跟进八个环节，你的把关点

| 环节 | Agent 会做什么 | 你的 checkpoint（必须把关） |
|------|--------------|--------------------------|
| **office-hours** | 追问需求细节，尤其数据、口径、兜底 | 回答它关于数据/口径的问题；确认"值得做" |
| **spec** | 死磕边界：失败模式、No-Go、数据模型、验收口径 | 审查 No-Go 是否具体；验收口径是否写进 SPEC |
| **autoplan** | 暴露两可决策，等你拍板 | **真的拍板**（准确率门槛、兜底策略、UI 取舍），写回 SPEC |
| **施工** | 按 SPEC 实现，每模块冒烟。**测试数据已在启动提示词里给出，Agent 会先落盘成 `tests/test_cases.csv` 再施工** | 抽查代码没偏离 spec 的 No-Go；确认测试集来自启动提示词、不是 Agent 自己编的 |
| **review** | 两遍检查（致命+信息），P0-P3 分级 | 确认 P0/P1 修复；API key 没硬编码；**审查是否覆盖两条关键坑：1）`.env` 配置解析异常（如 `CONFIDENCE_THRESHOLD` 配成非数字）有没有 try/except 兜底，避免启动即崩；2）模型返回缺字段时是否安全降级为"转人工"（而不是 KeyError 崩掉整批评估）** |
| **qa** | 真实浏览器跑核心流程 + 边界输入 | 五类都测；空文本/超长/口语/对抗样本；准确率达标。**耗时提示：批量评估串行 25 条约 2 分钟，建议先跑 CLI（`python -m evaluate`）拿结论，再用 UI 演示，不要反复自动化点滑块** |
| **ship** | 版本+CHANGELOG+README+commit | 确认 .env 未入库 |
| **retro** | 复盘四点 | 摩擦写具体；有改进行动项 |

### 关键决策点，Agent 停下来时你要拍板

Agent 可能会在这些地方等你，常见的有：

1. **验收口径**：如果 Agent 没在 office-hours 确认，你在 spec/autoplan 里要拍板"自信区 vs 整体准确率"——**项目已定：自信区 ≥85% + 转人工 ≤30%**
2. **准确率门槛**：85% 还是更高？——**快赢定位，85% 即可**
3. **兜底策略**：拿不准归"其他"还是"转人工"？——**已定：转人工**
4. **UI 取舍**：面板要不要做阈值调节滑块？——**按你的判断，但注意别卡在自动化测滑块上**
5. **No-Go 确认**：AI 想做的任何"spec 明确不做"的事，拒绝

> **FDE 灵魂追问 #1：** Agent 在 autoplan 停下来问"准确率门槛定多少"——你不能说"你看着办"。为什么？因为**85% 够不够，是业务决策**（坐席能不能接受这个误分率），AI 不知道，只有你结合实操一的方案框架知道。**决策权在人的原因，就在于人掌握 AI 没有的业务上下文。**

**✅ 检查点：**
- [ ] 八环节都跑完，每环节有产出文件
- [ ] 每次 Agent 停下要拍板时，你都真的做了决策（不是"你看着办"）
- [ ] SPEC 里验收口径写清楚了（自信区 ≥85% + 转人工 ≤30%）
- [ ] 代码没偏离 No-Go
- [ ] 准确率达标，边界输入测过
- [ ] .env 未入库

---

## 3. 验收与收尾（20 分钟）

### Step 1：核对交付物

对照下面清单，确认所有文件都在：

```
实操二-诉求分类/
├── 01-office-hours.md        # office-hours 确认记录
├── SPEC.md                   # 含失败模式 + No-Go + 数据模型 + 验收口径
├── SPEC-决策记录.md           # autoplan 拍板记录
├── src/                      # prompt / classify / evaluate / provider 等
├── streamlit_app.py          # Web 验证面板
├── tests/test_cases.csv      # ~25 条标注样本（每类≥5条）
├── review.md                 # 两遍审查 + P0-P3
├── qa-report.md              # 测试报告（含边界输入）
├── VERSION / CHANGELOG.md / README.md
└── retro.md                  # 复盘
```

### Step 2：演示给"坐席组长小王"看

模拟向坐席组长小王演示这个系统。要点：
- 用**她听得懂的话**讲（别说"零样本分类"）
- 演示"拿不准的转人工"这个兜底（对应她的角色升级）——"AI 分不准的会留给你判断，你不是被替代，是变成审核员"
- 让她看到"错分会减少"

> **FDE 灵魂追问 #2：** 向小王演示时，为什么"转人工兜底"是最好的切入点？——因为这是她最关心的"会不会被替代"的答案。**方案设计时留的"转人工"，不只是技术兜底，更是给一线坐席的"角色定位"。**

### Step 3：DoD 验收（演示级声明）

在 qa-report 或 retro 里，写一句**验收定位声明**：

> "本项目使用约 25 条测试样本，属**演示级验收**——结论仅证明方案在给定样本上可行，不宣称系统已达到生产可用。"

> **FDE 灵魂追问 #3：** 为什么必须写这句"演示级验收声明"？——**FDE 的第一戒律是不能夸大交付物。** 25 条样本全对，不代表 1.2 万条真实诉求全对。你敢把"准确率 100%"写成交付报告吗？写了，就是埋雷。

**✅ 检查点：**
- [ ] 所有交付物齐全
- [ ] 向小王演示成功，用了"转人工兜底"讲她的角色升级
- [ ] 写了"演示级验收声明"
- [ ] DoD 全过

---

## 4. 复盘沉淀（10 分钟）

### Step 1：看 Agent 的 retro 产出

Agent 跑 retro 时会产出复盘四点（做得好/摩擦/未覆盖/行动项）。

### Step 2：手动补充"对下一轮有用"的两条

在 retro.md 追加：

1. **AGENTS.md 建议**：如果 Agent 没生成，让它在项目根目录生成一份 AGENTS.md（项目硬约束、目录地图、验收命令、工具坑）——实操三（场景 B）开新会话时，这份文件能让 AI 直接继承上下文。

2. **改进行动项交接**：写下"下一轮（场景 B RAG）开工前，我要先看这条经验：____"

3. **面向 Lab4B 的能力抽象（长期交接）**：如果分类能力以后由 Agent 按需调用，最小输入输出应该是什么？哪些字段是西岭专属、哪些可以跨场景复用？建议参考接口（只记录、不要求在实操二完成服务化改造）：

```text
classify_request
输入：request_text
输出：category、confidence、reason、needs_human_review、status
```

> **衔接实操三：** 实操三开工时，新会话读实操二的 AGENTS.md + retro.md，就能继承"这个项目怎么跑、有哪些坑"的上下文——这就是 FDE 团队的经验回注（碎石路→铺装公路的微缩版）。
>
> **衔接 Lab4B（附加高级实验，可后置）：** 本实验记住一个判断——**分类在综合 Agent 中可用，但不必每次调用。** 不要让未来的 Agent 集成迫使你现在过度工程化；记录最小能力契约即可。

**✅ 检查点：**
- [ ] retro.md 追加了 AGENTS.md 建议
- [ ] 写了改进行动项交接（留给实操三）

---

## 5. 完成标准（DoD）

- [ ] gstack 已安装（Agent 装或手动装）
- [ ] 写了高质量的 office-hours 启动提示词（含数据现状 + 验收口径）
- [ ] Agent 按 gstack 八环节自动跑完，每环节有产出文件
- [ ] 每次拍板都真的做了决策（不是"你看着办"）
- [ ] 验收口径写进 SPEC：自信区 ≥85% + 转人工 ≤30%
- [ ] 分类系统可运行，验收达标
- [ ] 测试样本每类 ≥5 条（~25 条）
- [ ] .env 未入库，P0/P1 已修复
- [ ] 向小王演示成功（讲了"转人工"= 角色升级）
- [ ] 写了"演示级验收声明"
- [ ] retro.md 追加了 AGENTS.md 建议 + 交接行动项
- [ ] 3 个 FDE 灵魂追问参与讨论
- [ ] **已用一句话说明分类能力在 Lab4B 中的可选位置，并记录最小输入输出契约**
- [ ] 完成《Echo Prototype 检查单》（教科书 8.6）：错分代价分级 / 置信度与转人工策略确认 / 人工处理容量 / 演示反馈 / 需求满足判断

---

## 附录 A：gstack 安装速查

| 方式 | 操作 |
|------|------|
| Agent 自装 | 用第 0 节的提示词让 opencode 装 |
| 手动 | `git clone https://github.com/garrytan/gstack.git ~/gstack && cd ~/gstack && ./setup --host opencode` |
| 验证 | `~/.config/opencode/skills/` 下应有 office-hours/spec/autoplan/review/qa/ship/retro 等目录 |

## 附录 B：FDE 灵魂追问汇总

| # | 环节 | 追问 |
|---|------|------|
| 0 | 注入 | 为什么强调"走完一个环节就停下来等我确认"？一口气跑完你会失去什么？ |
| 1 | 注入跟进 | 拍板权为什么必须在人？AI 不知道什么？ |
| 2 | 验收收尾 | "转人工兜底"为什么是最好的向小王演示切入点？ |
| 3 | DoD | 为什么必须写"演示级验收声明"？夸大交付物的后果？ |

## 附录 C：讲师引导要点

- **环节 1（写启动提示词）是本轮教学核心**：不要让学生直接翻附录D。观察他们是否经历"思考—自查—改写"的过程。常见错误：漏掉"数据现状"（没写无历史标注 → 不会走零样本路线）、漏掉"验收口径"（agent 会自己发明）、选型理由没带过去（agent 可能又问你"用不用 RAG"）。这些漏项正是 Step 3 的 7 个自查问题要他们自己发现的。
- **环节 2 Step 1（注入）是本轮第二个教学点**：观察学员是否真的把启动提示词灌进 `/office-hours`，而不是直接丢给 Agent 一段话就完事。驱动指令的"一环一停 + 拍板点列选项"两条，决定了他们后面还有没有把关的机会。
- **环节 2 Step 2 关键看拍板质量**：Agent 停下时学员是否敷衍"你看着办"。这是"人掌握业务上下文"的核心体验，要盯。
- **提醒安装**：课前准备让 Agent 自装 gstack，避免卡在手动安装。Windows 用户注意路径是 `~/.config/opencode/skills/`。
- **样本统计教学点**：25 条样本（每类 5 条）让"各类统计"有了最低支撑，但 25 条仍是演示级——每条占 4%，85% 线 = 至少对 21/25。可趁机讲"小样本统计脆弱性"和"演示级验收"——这是 FDE 的素养。
- **provider 抽象（可选进阶）**：如果时间够，可引导学员做 provider 抽象（LLM_PROVIDER 环境变量切换 DeepSeek/硅基流动/GLM），体现"方案可替换"——但这属于加分项，不强求。

## 附录 D：启动提示词参考答案

> **⚠️ 仅供环节 1 完成后对照，不要先看。** 看这张表前，先确保你自己独立写完了一份。对照时重点看：漏了哪些信息？哪些是参考答案没有但你想到了的？

```
请以 FDE 团队的 Delta 角色，为西岭市民服务平台启动场景 A 的施工。

【背景】我们上一轮作为 Echo 完成了需求诊断，方案框架里场景 A 已选型
为"文本分类"（市民诉求 → 委办局类别），被定为快赢场景，现在由 Delta 施工。

【要做什么】市民诉求五分类系统：输入一句诉求文本，输出类别
（住建/人社/市监/城管/其他）。

【技术选型】已定文本分类，不用 RAG/Agent（一次调用即可，无需多步决策）。
技术栈：Python + DeepSeek API + Streamlit 验证面板。

【数据现状】无历史标注数据 → 必须走大模型零样本分类，不能用传统机器学习。

【测试集】请把以下测试数据写入 tests/test_cases.csv（含表头 id,text,label），施工时用它作为验收测试集，数据不得自行改动或重新生成：
id,text,label
1,我家门口的路灯坏了两个星期了，晚上一片漆黑，一直没人来修,住建
2,小区楼道的墙皮一直渗水，物业说管不了，让我自己找开发商,住建
3,开发商交房三年了，房产证还迟迟办不下来，找了好几次都没结果,住建
4,新买的房子天花板有裂缝，开发商只答应刷漆，想咨询能不能要求退房,住建
5,小区电梯频繁故障，物业说修不好，担心老人孩子坐电梯出事,住建
6,我是外地户口，想问问能不能在西岭市自己交社保,人社
7,公司连续三个月没给我交医保，现在离职了，想知道怎么自己续交,人社
8,失业金领取需要准备哪些材料？我下个月就离职了,人社
9,退休金怎么办理领取手续？我妈退休三个月了还没领到钱,人社
10,想申请创业担保贷款，需要满足什么条件，能补贴多少利息,人社
11,在超市买到过期食品，商家不承认是他们的问题,市监
12,美容院办卡后跑路了，我的预付卡还能要回钱吗,市监
13,网购的电器坏了，商家说是人为损坏拒绝保修,市监
14,健身房突然关门，会员卡还有两年有效期，想退钱,市监
15,菜市场有摊贩卖的猪肉好像没有检疫标志，该向哪里举报,市监
16,楼下烧烤店占道经营，晚上油烟和噪音吵得睡不着,城管
17,有人在小区绿地上私搭违建，已经举报过三次都没处理,城管
18,小区门口有人乱停共享单车，堵住了消防通道,城管
19,施工工地夜间施工扰民，城管管不管,城管
20,路边摊贩把污水直接倒在人行道上，又脏又滑,城管
21,想咨询一下市图书馆周末的开放时间是几点,其他
22,政务大厅下午几点下班？办理身份证要预约吗,其他
23,我家的狗丢了，想问问宠物走失该去哪里登记寻找,其他
24,想了解一下市民卡丢了怎么补办，需要哪些证件,其他
25,请问社区免费体检什么时间开始，需要带什么材料,其他

【验收口径】自信区准确率 ≥85%，转人工比例 ≤30%（转人工不算错）。

【约束】诉求文本可能含个人信息，注意不要硬编码任何密钥；
API key 走环境变量。

请按 gstack 标准开发流程（office-hours → spec → autoplan → build →
   review → qa → ship → retro）执行这次开发，每一环节产出文件，
并在我需要拍板的地方停下来问我。
```

**对照要点：**
- **必备的信息块**：背景、要做什么、输入输出、技术选型（含理由）、数据现状、**测试集（含落盘指令 + 25 条数据）**、验收口径、约束。你漏了哪块，对照前面 Step 1 的 7 项清单想——那是你写的时候没想到的。
- **参考答案里有两处"防翻车"信息**：1）"必须走零样本，不能用传统机器学习"（防 Agent 走死路）；2）"转人工不算错"（防验收口径自相矛盾）。你没写这两条的话，Agent 大概率会踩。
- **测试集数据**：25 条数据要原样附在启动提示词里，并明确"写入 `tests/test_cases.csv`、不得自行改动或重新生成"。这样 Agent 落盘后测试集是固定、干净的。
- **流程要求**：参考答案只在末尾轻提了一句"按流程执行"。真正要把流程"焊死"，靠的是环节 2 的驱动指令（5 条要求），两者配合才完整。
