# AGENTS.md — 本书的编写与迭代约定

> 本文档面向后续参与本书维护/迭代的协作者（含 AI 代理），说明**这套教科书以什么方式、什么格式编写**，以及**后续更新迭代应采用什么方法**，以保证全书一致性与可维护性。

---

## 1. 仓库概况

- **内容**：《人工智能工程师（高级）FDE 认证培训 · 教科书》——FDE 能力培养的**实战训练教科书**。
- **定位**：**独立成书**，不依赖《AI 时代的 FDE》（FDECore）。课程主体紧密相关的方法论直接写入本书；相关性不紧密的扩展内容只给在线全书链接。
- **在线全书（方法论大全，仅作链接）**：https://www.cloudzun.com/fde-course/ （《AI 时代的 FDE》，CC BY-NC-ND 4.0——本书与它同源不同书，**禁止复制其原文**）。
- **正文**：`textbook/` 下 17 个 markdown 文件（00 导读 + 第01–16章）。
- **核心主线**：Echo（靠想·判断力）→ Delta（靠做·施工力）双角色分工，西岭市民服务平台单案例贯穿；五个实操形成**总分总闭环**（Ch6 实操一【总】→ Ch8/10/12 实操二三四【分】→ Ch14 Delta 生产化（Build/Scale 纵切）→ Ch15 全程总览·价值落地与能力回注 → Ch16 实操五【总】）。
- **配套**：本文件（规范）、`misc/` 过程文档（交接/大纲/技术规格）、参考资料（`D:\vibecoding\dsh\FDECore\textbook\` 与 `D:\vibecoding\dsh\FDE实战训练营实验素材\` 仅作参考来源）。

---

## 2. 目录与文件规范

| 位置 | 内容 | 修改要求 |
| :--- | :--- | :--- |
| `textbook/` | 教科书正文（唯一工作底稿） | 正文增删改只在这里做 |
| `references/` | 参考资料库（行业/技术/算力调研，只读） | 只作来源，不修改 |
| `resources/` | 随书资源包（模板/工具速查） | 按需补充 |
| `misc/` | 中间过程文档（交接/大纲/技术规格） | 非最终交付物，归档备查 |
| `AGENTS.md`（本文件） | 编写与迭代约定 | 规范有变化时同步更新 |

**文件命名**：章节文件固定 `第NN章_<主题>.md`（两位数字序号）；导读为 `00_导读与体例说明.md`。**文件名一旦确定不要随意改名**（会破坏交叉引用）。

---

## 3. 写作格式规范（markdown 约定）

### 3.1 标题层级
- `#` = 章标题（`# 第 N 章  <标题>`）
- `##` = 节（`## N.x ...`）
- `###` = 子节（`### N.x.y ...`，仅当节内有独立子议题时使用）
- 禁止跳级（如 `#` 直接到 `##` 下的 `###` 跳级）。

### 3.2 每章统一体例（章节模板，全书一致）
```
# 第 N 章  <标题>
> 本章定位 / 本章产出 / 建议读者 / 前置（/ 适配说明，实操单元）

## 本章导学（学习目标 / 前置知识 / 章节地图）
## 正文（小节，图表 + mermaid 穿插）
## 反模式与红线
## 本章小结（+ 动手自检清单）
## 练习与思考（基础 / 进阶 / 挑战）
## 在线全书（延伸阅读，统一格式）
```
实操单元额外包含：任务书 → 演练流程 → 产出物清单 → 验收标准（DoD）→ 适配说明。

### 3.3 引用块（GitHub Alert 语法，按语义选用）
- `> [!NOTE]` 一般提示 / 定位说明
- `> [!TIP]` 操作建议
- `> [!IMPORTANT]` 关键结论 / 纪律
- `> [!CAUTION]` 风险提醒 / 反面模式

### 3.4 mermaid 布局规则（重要，全书统一）
- **元素（节点）超过 4 个：一律用垂直布局（`flowchart TB` / `TD`，自上而下）**；
- 元素 ≤ 4 个：才允许水平布局（`flowchart LR`）。
- 循环图（如飞轮）即便元素多也按垂直成环处理。
- 图下必须带图题：`*图 N-x：标题*`。
- 配色沿用 FDECore 风格：`#3949ab`（靛蓝主色）、`#f5ece0`（琥珀/米色）、`#e4efe4`（绿色）、`#fdecea`（红色警示）、`#dbe4f0`（浅蓝）。

### 3.5 表格
- 必须带表头分隔行；
- 长表格附"读法指引/使用说明"。

### 3.6 检查清单
- Gate 检查清单、验收标准（DoD）、自检清单用 `- [ ]` 复选框。

### 3.7 中文风格
- 简体中文，正文用全角标点；
- 术语首次出现给出英文原文（如 FDE（Forward Deployed Engineer，前沿部署工程师））。

---

## 4. 内容归属原则（本书与在线全书的分工）

> 这是本书区别于"FDECore 参考书"的核心纪律。

| 内容 | 处理方式 |
|------|---------|
| 与**课程主体紧密相关**（FDE 是什么/为什么、交付方法论、Echo/Delta 分工、Echo 怎么干活、五个实操） | **直接写入本书**，颗粒度对齐、图表齐全，本书能独立读懂 |
| 与课程相关性不紧密（Palantir 平台体系细节、完整案例集、中国云厂商图谱、人才培养/HR、甲方合同定价、附录术语/合规速查） | **只给链接**，不作为本书正文 |
| 引用 FDECore 概念 | 一律以"在线全书链接 + 章节名称"形式出现，**不写"延伸阅读 → FDECore 第 X 章"的依赖式表述** |

**链接统一格式**（全书一致，不给章节锚点）：
```
见在线全书 https://www.cloudzun.com/fde-course/（第 X 章 <章节名称>）
```

---

## 5. 内容纪律（最终交付物视角）

### 5.1 无"内部解释性"表述（红线级）
> 读者拿到的是**最终版本**，不知道我们内部思路的演变。以下内容**禁止**出现在正文：
- ❌ "我们从问题本身讲起，而不是从 X 讲起"（向内部解释叙事选择）
- ❌ "这是 1.1 想纠正的最大误区"（内部指引）
- ❌ "这不是'软件买来用不起来'的问题"（与旧叙事对比的否定式）
- ❌ "本书同时服务两类读者……殊途同归"（内部定位解释）
- ❌ 任何透露"我们改过/换过方案"的痕迹
- ✅ 一切表述直接写给读者，正向陈述，读者不知所以的话不写。

### 5.2 双读者视角（默认视角）
- 本书侧重**服务提供方（乙方 / 独立服务商）** 与 **甲方内部服务交付团队（如银行总行科技部）** 两类读者；
- 涉及"客户""卖人力→卖能力"等概念时，可给两类读者各一句落点（如 1.5 的双视角 NOTE），但要用直接陈述、不要内部口吻；
- 后文"客户"可理解为"外部客户"或"内部被服务的业务部门"。

### 5.3 案例选择
- 案例要有中国读者共鸣（如供应链/制造业/银行），避免"提升效率到正常水平"类对中国读者无感的案例（如 NHS 排队改善）；
- 优先能体现 FDE 独特价值的案例（业务关系网、能力回注、敏捷决策），而非"把烂摊子收拾正常"。

---

## 6. 术语统一表（新增内容必须沿用）

| 术语 | 全书统一口径 |
| :--- | :--- |
| FDE | Forward Deployed Engineer，前沿部署工程师 |
| 三层含义 | 岗位 / 团队 / 方法论 |
| 三重角色 | 全栈工程师 × 业务顾问 × 产品经理（方法论是形、工程师是神） |
| 卖人力 → 卖能力 | 全书主线（对外卖价值 / 对内沉淀能力中台） |
| 四条红线 | 数据不出域 / 答案可追溯 / 敏感件零漏判 / 人能判断·AI 能执行 |
| 四阶段 | Discovery → Prototype → Build → Scale（教材提炼） |
| PPT 框架 | People–Process–Technology（人员流程技术），与演示文稿无关 |
| 能力回注 | 识别 → 抽象 → 集成 → 验证（四步法） |
| 角色 | 三角色（Delta / Echo / Engineering）；Echo 靠想、Delta 靠做 |
| 三个脚手架 | 四类失败模式 / 四层决策链 / LLM 能力金字塔 |
| 三个 SOP | 干系人地图（SOP1）/ 数据资产盘点（SOP2）/ 快赢筛选（SOP3） |
| 五实操 | 实操一【总】→ 二三四【分】→ 五【总】（总分总闭环） |
| 西岭案例 | 西岭市民服务平台（单案例贯穿） |

---

## 7. 编号纪律

- 新增小节用**顺延编号**（现有到 N.x，新增 N.x+1），不占用已有编号；
- **一旦重编号**，必须全文 grep `N\.[0-9]` 检索交叉引用并同步更新；
- 交叉引用一律写"第 N 章 N.x"或"（N.x）"，不写模糊的"前文/后文"。

---

## 8. 溯源纪律

- **官方/事实性引文**：必须有可点击 URL 或注明出处（如 Palantir 财报、FDSE 招聘 JD）；
- **教材提炼**：明确标注"教材提炼"，不冒充厂商官方术语；
- **行业数字**：注明来源与时间点；无来源的量化断言要么给来源、要么弱化为定性并标注"经验表述/待核验"；
- **时效性内容**（财务数据、行业事件等）：标注"信息截至 YYYY 年 MM 月"，落地前自行核对。

---

## 9. 版本与修订流程

### 9.1 版本号
- `v0.x` 迭代（当前 v0.7：按专家评审意见逐章修订中，修订记录以 `textbook/00_导读与体例说明.md` 为准）；
- 每个版本阶段在 `textbook/00_导读与体例说明.md` 的修订记录登记：版本号 → 阶段名 → 改动摘要 → 联动更新项。

### 9.2 修订模式（分批，避免一次大改）
1. **盘点问题**：列问题清单，按严重度分 🔴（事实/引用/计数）→ 🟡（口径/信源）→ 🟢（锦上添花）。
2. **分批执行**：一批做完 → 全局核验（编号连续性 + 交叉引用 grep + 旧表述残留）→ 再开下一批。
3. **核验清单**：
   - 新增小节编号连续无重复；
   - 全文 grep `N\.[0-9]` 交叉引用全部有效；
   - 旧表述/内部解释性文字零残留（按 5.1 搜索）；
   - mermaid 图均满足 3.4 布局规则（>4 元素为垂直）；
   - 链接均按 4 节的统一格式；
   - 新增内容的引用目标小节确实存在；
   - 更新 `00_导读与体例说明.md` 修订记录。

### 9.3 内容增删改 SOP
- **新增章节/小节**：确定编号 → 按 3.x 写 → grep 交叉引用 → 更新导读。
- **修改既有内容**：改 `textbook/` 分文件 → 同步检查相关引用 → 记录版本。
- **重写章节**：遵守第 3–8 节全部纪律，改完跑核验清单。

---

## 10. 迭代更新 SOP（给 AI 代理/协作者的执行清单）

每次迭代按以下顺序执行：

1. **读上下文**：先读本文件（规范）+ `textbook/00_导读与体例说明.md`（版本与清单）+ 目标章节文件。
2. **定位改动点**：明确"改哪个文件、哪个小节、为什么"；涉及编号/引用的先做影响面分析。
3. **写**：遵守第 3–8 节全部格式与纪律。
4. **核验**：按 9.2 核验清单跑 grep；修正发现的问题。
5. **记录**：更新 `textbook/00_导读与体例说明.md` 修订记录（版本号 + 摘要 + 联动项）。
6. **汇报**：列出改动文件、改动位置、核验结果。

---

## 11. 红线（必须遵守）

- **不复制在线全书（FDECore）原文**（CC BY-NC-ND）；只引用链接。
- **不写"延伸阅读 → FDECore 第 X 章"的依赖式表述**；本书必须独立可读。
- **不出现内部解释性表述**（按 5.1，读者视角 = 最终交付物视角）。
- **mermaid 不违反布局规则**（>4 元素必须垂直）。
- **不新增不可溯源的"官方引文"**。
- **不引入与既有主线矛盾的新概念**：新内容须与"卖人力→卖能力"主线、四条红线、Echo/Delta 分工、四阶段、五实操总分总、双读者视角保持一致。
