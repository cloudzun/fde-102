# AGENTS.md — 本书的编写与迭代约定

> 本文档面向后续参与本书维护/迭代的协作者（含 AI 代理），说明**这套教科书以什么方式、什么格式编写**，以及**后续更新迭代应采用什么方法**，以保证全书一致性与可维护性。

---

## 1. 仓库概况

- **内容**：《FDE 实战：Echo 与 Delta 的交付之道》——FDE 能力培养的**实战教科书**（副题：把问题变成答案，把定制变成能力）。
- **定位**：**独立成书**，不依赖《AI 时代的 FDE》（FDECore）。课程主体紧密相关的方法论直接写入本书；相关性不紧密的扩展内容只给 **FDE-101** 链接。
- **FDE-101（方法论大全，仅作链接）**：https://www.cloudzun.com/fde-course/ （《AI 时代的 FDE》，CC BY-NC-ND 4.0——本书与它同源不同书，**禁止复制其原文**）。
- **正文**：`textbook/` 下 17 个 markdown 文件（00 导读 + 第01–16章）。
- **核心主线**：Echo（主责判断·判断力）→ Delta（主责施工·施工力）双角色分工，西岭市民服务平台单案例贯穿；五个实操形成**总分总闭环**（Ch6 实操一【总】→ Ch8/10/12 实操二三四【分】→ Ch14 Delta 生产化·工程 Scale（生产落地与客户接管）→ Ch15 Echo 价值化·业务 Scale（价值落地、组织采纳与能力回注） → Ch16 实操五【总】）；另有**无编号实操四B（Lab4B）**（三个单点 MVP 与综合收口间的受控 Agent 桥梁）与**无编号可选"复盘真实项目"**（课程结束后出口），均不占章号、不影响主线结业。
- **配套**：本文件（规范）、`misc/` 过程文档（交接/大纲/技术规格）、参考资料（`D:\vibecoding\dsh\FDECore\textbook\` 与 `D:\vibecoding\dsh\FDE实战训练营实验素材\` 仅作参考来源）。

---

## 2. 目录与文件规范

| 位置 | 内容 | 修改要求 |
| :--- | :--- | :--- |
| `textbook/` | 教科书正文（唯一工作底稿） | 正文增删改只在这里做 |
| `references/` | 参考资料库（行业/技术/算力调研，只读） | 只作来源，不修改 |
| `resources/` | 随书资源包（模板/工具速查） | 按需补充 |
| `labs/` | 实验手册正式册（英文路径，与 `textbook/` 同级）：实操一~五学员版 + 实操四B + 复盘真实项目 + 客户需求/政策素材/gstack 流程 | 自 2026-08 起**本册为实验手册权威来源**（陕西移动专题综合实操为历史参考，不回写）；与教科书实操章**错位分工**：手册承载执行细节（步骤/参考答案/模板/素材），教科书承载方法与验收；正文引用本册统一用"见《实验手册》`labs/<文件名>`" |
| `misc/` | 中间过程文档（交接/大纲/技术规格） | 非最终交付物，归档备查 |
| `scripts/` | 构建与校验脚本：`build_docs.py`（`textbook/` → `docs/`）、`style-lint.py`（格式与体例一致性检查，零依赖）、`mermaid-dark-node-check.js`（mermaid 深底深字回归，Node + jsdom，依赖见 `scripts/package.json`） | 站点构建与自动检查入口；改渲染相关资源后按 9.2 跑校验 |
| `AGENTS.md`（本文件） | 编写与迭代约定 | 规范有变化时同步更新 |

**文件命名**：章节文件固定 `第NN章_<主题>.md`（两位数字序号）；导读为 `00_导读与体例说明.md`。**文件名一旦确定不要随意改名**（会破坏交叉引用）。

---

## 3. 写作格式规范（markdown 约定）

### 3.1 标题层级
- `#` = 章标题（`# 第 N 章  <标题>`）
- `##` = 节（`## N.x ...`）
- `###` = 子节（`### N.x.y ...`，仅当节内有独立子议题时使用）
- 禁止跳级（如 `#` 直接到 `##` 下的 `###` 跳级）。

### 3.2 每章统一体例（章节模板，全书一致）
**章首只保留一套导引**：引用块（本章定位 / 建议读者 / 前置）+「本章学习目标」编号列表。
```
# 第 N 章  <标题>
> 本章定位 / 建议读者 / 前置（/ 承接、实验手册、定位说明等章别专用字段；实操单元另有适配说明）

**本章学习目标：**
1. …（编号列表，条数与章节内容匹配）

> **一句话记住本章：** ……（可选，全章点睛）
**学习目标追踪（目标 → 活动 → 证据 → 验收）：**（实操章可选，一张表）

## 正文（小节，图表 + mermaid 穿插）
## 反模式与红线
## 本章小结（+ 动手自检清单）
## 练习与思考（基础 / 进阶 / 挑战）
## 延伸阅读（**FDE-101** 链接，统一格式）
```
实操单元额外包含：任务书 → 演练流程 → 产出物清单 → 验收标准（DoD）→ 适配说明。

**实操章节序（全书统一，2026-09 起）：**
```
## N.1 演练背景        （情景与任务导入）
## N.2 知识背景        （本节所需方法）
## N.3 任务书          （材料与要求）
## N.4 课前准备        （环境 / 分组 / 材料）
## N.5 演练流程
## N.6 产出物清单与验收标准（DoD）
## N.7 Echo Prototype 检查单
```
节名不得加副标题（如「知识背景：三个思维脚手架」→「知识背景」）。**两处例外**：① 实操一（纯头脑风暴、无技术产出）无「课前准备」与「Echo Prototype 检查单」，首节用「演练背景」；② 实操五（评审汇报工作坊）用「演练前提」代替「课前准备」，无「任务书」与检查单，并在「延伸阅读」之后保留「## 全书收口的话」作为全书结束语。

**章首纪律（2026-09 起，全书统一）：**
- **不设 `## 本章导学` 节**，"学习目标 / 前置知识 / 章节地图"三个字段随之取消——它们与引用块的字段**内容重叠、形式雷同**；
- **不写「本章产出」**：产出即学习目标的落地结果，写进「本章学习目标」即可；
- **章首不配 mermaid 导航图**：章节在全书中的位置用「本章定位」一句话讲清，不用"画图 + 正文再讲一遍"重复一遍（**正文配图不受此限**）；
- 章别专用字段（承接、实验手册、定位说明、实验设置、本章属性、核心隐喻、延伸阅读）保留在引用块内。

### 3.3 引用块（GitHub Alert 语法，按语义选用）
- `> [!NOTE]` 一般提示 / 定位说明 / 口径说明
- `> [!TIP]` 操作建议
- `> [!IMPORTANT]` 关键结论 / 红线落点 / 重要前提
- `> [!CAUTION]` 风险提醒 / 反面模式
- **语义标签升格为标题**：写「注意 / 提示 / 反模式 / 口径说明」这类短标签时，用 `> [!NOTE] 注意` 的形式（标签即标题），**不要**写成裸引用块 `> **注意：** …` 再让 Alert 显示默认标题；正文首行不再重复该标签。
- **先查是否已在 Alert 内**：本节的「红线本处落地」块**本身就是** `> [!IMPORTANT]` + 内容，不要再补标记（曾因此产生连续标记）。改前先看块的上方一行。
- 连续两个 `> [!X]` 标记属缺陷（渲染为空框），须清理。

### 3.4 mermaid 布局规则（重要，全书统一）
- **元素（节点）超过 4 个：一律用垂直布局（`flowchart TB` / `TD`，自上而下）**；
- 元素 ≤ 4 个：才允许水平布局（`flowchart LR`）。
- 循环图（如飞轮）即便元素多也按垂直成环处理。
- 图下必须带图题：`*图 N-x：标题*`。
- 配色沿用 FDECore 风格：`#3949ab`（靛蓝主色）、`#f5ece0`（琥珀/米色）、`#e4efe4`（绿色）、`#fdecea`（红色警示）、`#dbe4f0`（浅蓝）。
- **深色填充节点必须白字**：Material 把 mermaid 放进 closed shadow DOM 并注入 `.nodeLabel p{color:深色}`，源码里的 `color:#fff` 到不了内层 `<p>`，因此由 `javascripts/mermaid-themefix.js` 追加 `themeCSS` 覆盖。该文件按**节点形状**写选择器（`SHAPES`），**新增形状必须同步补上**——曾因只列 `rect` 导致 `<polygon>` 决策节点深底深字。改完跑 `node scripts/mermaid-dark-node-check.js` 回归（见 9.2）。

### 3.5 表格
- 必须带表头分隔行；
- 长表格附"读法指引/使用说明"；
- **表格不编号**：图有图题 `*图 N-x：标题*`，表**不用** `*表 N-x*`（全书仅 2 处历史遗留可保留），需要指路时用"上表 / 下表 + 就近引出句"，不写"见表 3-1"；
- **表内时点标注**：型号/价格/版本等易变数据的表，可在表头或表注写一次口径时间（如"官方 API 报价（2026-09）"）；**同一页不要重复标注**。

### 3.6 标点与符号体例（2026-09 起，全书统一）
- **并列分隔**用 ` / `（斜杠前后各一个**半角空格**）；**例外**：单位（`699元/月`）、文件路径（`labs/lab2.md`）、比例（`6/6`）、复合词（`本地/微调`）保持紧凑不加空格。
- **序列连接**：流程 / 步骤时序用 `→`（`识别 → 抽象 → 集成 → 验证`）；业务实体关系链用连接号 `—`（`诉求—政策条款—政策文件`）；数值区间用 en dash `–`（`20–30 分钟`），**不用** `—`。
- **引号**：中文引号沿用全书既有口径的半角 `"`（与参考站 fde-course 一致）；术语标签可用直角引号 `「」`；两者都不要与另一半混用。
- **破折号**用中文双破折号 `——`；单 `—` 仅用于上面说的实体关系连接。
- **中英文之间不加空格以外的分隔**；术语首次出现给英文原文（见 3.8）。

### 3.7 检查清单
- Gate 检查清单、验收标准（DoD）、自检清单用 `- [ ]` 复选框。
- **列表前必须空行**：`- [ ]` 列表（含普通无序列表）与上一段文字之间必须有**空行**隔开，否则 markdown 会把列表行并入上一段落、渲染成纯文本（checkbox 不生效）。标题行（如 `**动手自检…**`）与首个列表项之间同样需要空行。
- **嵌套子列表缩进 4 空格**：列表项（如 `- **步骤**：`、`- **学习目标追踪：**`）下的下一级列表，必须缩进 **4 个空格**；只缩进 2 空格时 Python-Markdown 不会识别为子列表，会把各项拆成并列的顶层项（渲染散开）。

### 3.8 中文风格
- 简体中文，正文用全角标点（引号除外，见 3.6）；
- 术语首次出现给出英文原文（如 FDE（Forward Deployed Engineer，前沿部署工程师））。

---

## 4. 内容归属原则（本书与 **FDE-101** 的分工）

> 这是本书区别于"FDECore 参考书"的核心纪律。

| 内容 | 处理方式 |
|------|---------|
| 与**课程主体紧密相关**（FDE 是什么/为什么、交付方法论、Echo/Delta 分工、Echo 怎么干活、五个实操） | **直接写入本书**，颗粒度对齐、图表齐全，本书能独立读懂 |
| 与课程相关性不紧密（Palantir 平台体系细节、完整案例集、中国云厂商图谱、人才培养/HR、甲方合同定价、附录术语/合规速查） | **只给链接**，不作为本书正文 |
| 引用 FDECore 概念 | 一律以"**FDE-101** 链接 + 章节名称"形式出现，**不写"延伸阅读 → FDECore 第 X 章"的依赖式表述** |

**链接统一格式**（全书一致，不给章节锚点）：
```
见 **FDE-101** https://www.cloudzun.com/fde-course/（第 X 章 <章节名称>）
```

---

## 5. 内容纪律（最终交付物视角）

### 5.1 无"内部解释性"表述（红线级）
> 读者拿到的是**最终版本**，不知道我们内部思路的演变。以下内容**禁止**出现在正文：
- ❌ "我们从问题本身讲起，而不是从 X 讲起"（向内部解释叙事选择）
- ❌ "这是 1.1 想纠正的最大误区"（内部指引）
- ❌ "这不是'软件买来用不起来'的问题"（与旧叙事对比的否定式）
- ❌ "本书同时服务两类读者……殊途同归"（内部定位解释）
- ❌ 任何透露"我们改过/换过方案"的痕迹
- ✅ 一切表述直接写给读者，正向陈述，读者不知所以的话不写。

### 5.2 双读者视角（默认视角）
- 本书侧重**服务提供方（乙方 / 独立服务商）** 与 **甲方内部服务交付团队（如银行总行科技部）** 两类读者；
- 涉及"客户""卖人力→卖能力"等概念时，可给两类读者各一句落点（如 1.5 的双视角 NOTE），但要用直接陈述、不要内部口吻；
- 后文"客户"可理解为"外部客户"或"内部被服务的业务部门"。

### 5.3 案例选择
- 案例要有中国读者共鸣（如供应链/制造业/银行），避免"提升效率到正常水平"类对中国读者无感的案例（如 NHS 排队改善）；
- 优先能体现 FDE 独特价值的案例（业务关系网、能力回注、敏捷决策），而非"把烂摊子收拾正常"。

---

## 6. 术语统一表（新增内容必须沿用）

| 术语 | 全书统一口径 |
| :--- | :--- |
| FDE | Forward Deployed Engineer，前沿部署工程师 |
| 三层含义 | 岗位 / 团队 / 方法论 |
| 三重角色 | 全栈工程师 × 业务顾问 × 产品经理（方法论是形、工程师是神） |
| 卖人力 → 卖能力 | 全书主线（对外卖价值 / 对内沉淀能力中台） |
| 四条红线 | 数据不出域 / 答案可追溯 / 敏感件零漏判 / 人能判断·AI 能执行 |
| 四阶段 | Discovery → Prototype → Build → Scale（教材提炼） |
| PPT 框架 | People–Process–Technology（人员流程技术），与演示文稿无关 |
| 能力回注 | 识别 → 抽象 → 集成 → 验证（四步法） |
| 本体建模 / 业务语义回注 | 把业务对象、属性、关系、状态、动作与治理约束沉淀为可复用业务语言（教材提炼；Palantir Ontology 为代表实现而非唯一标准） |
| 角色 | 三角色（Delta / Echo / Engineering）；Echo 主责判断、Delta 主责施工 |
| 三个脚手架 | 四类失败模式 / 四层决策链 / LLM 能力金字塔 |
| 三个 SOP | 干系人地图（SOP1）/ 数据资产盘点（SOP2）/ 快赢筛选（SOP3） |
| 五实操 | 实操一【总】→ 二三四【分】→ 五【总】（总分总闭环） |
| **FDE-101** | 在线方法论大全：https://www.cloudzun.com/fde-course/ （《AI 时代的 FDE》；本书与它同源不同书——仅作链接、不复制原文，不写依赖式表述） |
| 实操四B（Lab4B） | 无编号附加高级实验：把政策检索/部门查询/主动追问/工单草稿/人工审核组织为受控协同 Agent（第 12 章与第 16 章之间的桥梁）；不占章号、非场景 D |
| 复盘真实项目 | 无编号可选迁移实验（课程结束后出口）：用同一套 Echo/Delta 方法对本人真实项目做拆解、复盘、决策与汇报，不影响主线结业 |
| 西岭案例 | 西岭市民服务平台（单案例贯穿） |

---

## 7. 编号纪律

- 新增小节用**顺延编号**（现有到 N.x，新增 N.x+1），不占用已有编号；
- **一旦重编号**，必须全文 grep `N\.[0-9]` 检索交叉引用并同步更新。
- **引用克制纪律（2026-09 起）**：
  - **本章内不标注小节号**（知识点在本章循序渐进展开，无需"见 N.x"式引用）；
  - **跨章引用只保留核心概念与基本流程**：Echo/Delta 分工、四阶段交付、四条红线、能力回注、"卖人力→卖能力"主线、五实操总分总收口；**其余跨章引用一律不写**；
  - 保留**图 N-x** 的就近指引（表不编号，见 3.5）；保留章首体例字段（本章定位/建议读者/前置）；
  - **「下一章」统一格式**：写在「延伸阅读」最后一条，`- **下一章：** <章标题>（<一句说明>）`，不带章号；末章（第 16 章）不写；章内正文不再另写过渡句（避免与这条重复）。

---

## 8. 溯源纪律

> 口径调整（2026-09）：**不再在正文逐处插入来源性质标注**——作者要求清理影响阅读的过程性标注；来源性质改由 `00_导读与体例说明.md` 的统一声明交代。

- **外部来源链接必须保留**：官方/事实性引文必须有可点击 URL 或注明出处（如 Palantir 财报、FDSE 招聘 JD）；外部 URL 不因"清理引用"而删除。
- **不在正文逐处标注来源性质**：不写"教材提炼 / 教材经验口径 / 教学假设 / 待核验 / 信息截至 YYYY 年 MM 月"这类过程性标注，其性质由 `00_导读` 的一次性总声明统一说明。
- **教材提炼不冒充官方**：叙述方法论框架时不得声称其为某厂商官方术语或官方流程；若为教材提炼，按导读总声明处理即可。
- **动态数据以官方页为准**：型号、价格、许可证等易变数据，正文只给结论与查阅位置，落地前自行按官方页核对。
- **禁止无来源的"官方引文"**：不得编造或把转述写成官方原话；引用官方原话须给出处。

---

## 9. 版本与修订流程

### 9.1 版本号
- `v0.x` 迭代（当前 **v0.8**：引用克制与章首体例轮，修订记录以 `textbook/00_导读与体例说明.md` 的「## 修订记录」为准）；
- 每个版本阶段在 `textbook/00_导读与体例说明.md` 的修订记录登记：版本号 → 阶段名 → 改动摘要 → 联动更新项。**该节位于导读末尾，只追加、不删改历史条目。**

### 9.2 修订模式（分批，避免一次大改）
1. **盘点问题**：列问题清单，按严重度分 🔴（事实/引用/计数）→ 🟡（口径/信源）→ 🟢（锦上添花）。
2. **分批执行**：一批做完 → 全局核验（编号连续性 + 交叉引用 grep + 旧表述残留）→ 再开下一批。
3. **核验清单**：
   - 新增小节编号连续无重复；
   - 全文 grep `N\.[0-9]` 交叉引用全部有效；
   - 旧表述/内部解释性文字零残留（按 5.1 搜索）；
   - **体例检查通过**：`python scripts/style-lint.py`（零依赖）——覆盖固定尾部节齐全与顺序、`N.x` 编号连续、裸代码块、连续 Alert、语义标签式裸引用块、混合分隔链、单位被拆、图号连续、`下一章` 覆盖、章首字段、文件尾换行、过程性标注、标题中英空格、复选框前空行、嵌套缩进；退出码 0 才算过；
   - mermaid 图均满足 3.4 布局规则（>4 元素为垂直）；
   - **mermaid 深底深字回归通过**：`node scripts/mermaid-dark-node-check.js`（需先 `mkdocs build` 生成 `site/`，并在 `scripts/` 下 `npm install` 装 jsdom）；退出码 0 才算过；
   - 链接均按 4 节的统一格式；
   - 新增内容的引用目标小节确实存在；
   - 更新 `00_导读与体例说明.md` 修订记录。

### 9.3 内容增删改 SOP
- **新增章节/小节**：确定编号 → 按 3.x 写 → grep 交叉引用 → 更新导读。
- **修改既有内容**：改 `textbook/` 分文件 → 同步检查相关引用 → 记录版本。
- **重写章节**：遵守第 3–8 节全部纪律，改完跑核验清单。

---

## 10. 迭代更新 SOP（给 AI 代理/协作者的执行清单）

每次迭代按以下顺序执行：

1. **读上下文**：先读本文件（规范）+ `textbook/00_导读与体例说明.md`（版本与清单）+ 目标章节文件。
2. **定位改动点**：明确"改哪个文件、哪个小节、为什么"；涉及编号/引用的先做影响面分析。
3. **写**：遵守第 3–8 节全部格式与纪律。
4. **核验**：按 9.2 核验清单跑 grep；修正发现的问题。
5. **记录**：更新 `textbook/00_导读与体例说明.md` 修订记录（版本号 + 摘要 + 联动项）。
6. **汇报**：列出改动文件、改动位置、核验结果。

---

## 11. 红线（必须遵守）

- **不复制 FDE-101（FDECore）原文**（CC BY-NC-ND）；只引用链接。
- **不写"延伸阅读 → FDECore 第 X 章"的依赖式表述**；本书必须独立可读。
- **不出现内部解释性表述**（按 5.1，读者视角 = 最终交付物视角）。
- **mermaid 不违反布局规则**（>4 元素必须垂直）。
- **不新增不可溯源的"官方引文"**。
- **不引入与既有主线矛盾的新概念**：新内容须与"卖人力→卖能力"主线、四条红线、Echo/Delta 分工、四阶段、五实操总分总、双读者视角保持一致。
